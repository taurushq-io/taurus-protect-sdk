// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: rule.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Currency } from "./currency";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface BusinessRule {
  id: number;
  tenantId: number;
  currency: string;
  /**
   * Deprecated. This field will be empty from v3.32. Use entityType and entityID instead.
   *
   * @deprecated
   */
  walletId: number;
  ruleKey: string;
  ruleValue: string;
  ruleGroup: string;
  ruleDescription: string;
  ruleValidation: string;
  /**
   * Deprecated. This field will be empty from v3.32. Use entityType and entityID instead.
   *
   * @deprecated
   */
  addressId: number;
  currencyInfo:
    | Currency
    | undefined;
  /** entityType indicates what this rule applies to. This can be one of `global`, `currency`, `wallet`, `address`, `exchange`, `exchange_account`, `tn_participant`. */
  entityType: string;
  /** entityID is the identifier of the affected entity. For wallets, addresses, and currencies this is their ID. For exchanges this is the exchange label. This field is blank for global rules. */
  entityID: string;
}

export interface Rules {
  rulesContainer: string;
  rulesSignatures: RuleUserSignature[];
  locked: boolean;
  creationDate: Date | undefined;
  updateDate: Date | undefined;
  trails: Rules_Trail[];
}

export interface Rules_Trail {
  id: number;
  userId: number;
  externalUserId: string;
  action: string;
  comment: string;
  date: Date | undefined;
}

export interface RuleUserSignature {
  userId: string;
  signature: string;
}

function createBaseBusinessRule(): BusinessRule {
  return {
    id: 0,
    tenantId: 0,
    currency: "",
    walletId: 0,
    ruleKey: "",
    ruleValue: "",
    ruleGroup: "",
    ruleDescription: "",
    ruleValidation: "",
    addressId: 0,
    currencyInfo: undefined,
    entityType: "",
    entityID: "",
  };
}

export const BusinessRule = {
  encode(message: BusinessRule, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.tenantId !== 0) {
      writer.uint32(16).uint64(message.tenantId);
    }
    if (message.currency !== "") {
      writer.uint32(26).string(message.currency);
    }
    if (message.walletId !== 0) {
      writer.uint32(32).uint64(message.walletId);
    }
    if (message.ruleKey !== "") {
      writer.uint32(42).string(message.ruleKey);
    }
    if (message.ruleValue !== "") {
      writer.uint32(50).string(message.ruleValue);
    }
    if (message.ruleGroup !== "") {
      writer.uint32(58).string(message.ruleGroup);
    }
    if (message.ruleDescription !== "") {
      writer.uint32(66).string(message.ruleDescription);
    }
    if (message.ruleValidation !== "") {
      writer.uint32(74).string(message.ruleValidation);
    }
    if (message.addressId !== 0) {
      writer.uint32(80).uint64(message.addressId);
    }
    if (message.currencyInfo !== undefined) {
      Currency.encode(message.currencyInfo, writer.uint32(90).fork()).ldelim();
    }
    if (message.entityType !== "") {
      writer.uint32(98).string(message.entityType);
    }
    if (message.entityID !== "") {
      writer.uint32(106).string(message.entityID);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BusinessRule {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBusinessRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.currency = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.walletId = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.ruleKey = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.ruleValue = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.ruleGroup = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.ruleDescription = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.ruleValidation = reader.string();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.addressId = longToNumber(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.currencyInfo = Currency.decode(reader, reader.uint32());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.entityType = reader.string();
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.entityID = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BusinessRule {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      walletId: isSet(object.walletId) ? globalThis.Number(object.walletId) : 0,
      ruleKey: isSet(object.ruleKey) ? globalThis.String(object.ruleKey) : "",
      ruleValue: isSet(object.ruleValue) ? globalThis.String(object.ruleValue) : "",
      ruleGroup: isSet(object.ruleGroup) ? globalThis.String(object.ruleGroup) : "",
      ruleDescription: isSet(object.ruleDescription) ? globalThis.String(object.ruleDescription) : "",
      ruleValidation: isSet(object.ruleValidation) ? globalThis.String(object.ruleValidation) : "",
      addressId: isSet(object.addressId) ? globalThis.Number(object.addressId) : 0,
      currencyInfo: isSet(object.currencyInfo) ? Currency.fromJSON(object.currencyInfo) : undefined,
      entityType: isSet(object.entityType) ? globalThis.String(object.entityType) : "",
      entityID: isSet(object.entityID) ? globalThis.String(object.entityID) : "",
    };
  },

  toJSON(message: BusinessRule): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.walletId !== 0) {
      obj.walletId = Math.round(message.walletId);
    }
    if (message.ruleKey !== "") {
      obj.ruleKey = message.ruleKey;
    }
    if (message.ruleValue !== "") {
      obj.ruleValue = message.ruleValue;
    }
    if (message.ruleGroup !== "") {
      obj.ruleGroup = message.ruleGroup;
    }
    if (message.ruleDescription !== "") {
      obj.ruleDescription = message.ruleDescription;
    }
    if (message.ruleValidation !== "") {
      obj.ruleValidation = message.ruleValidation;
    }
    if (message.addressId !== 0) {
      obj.addressId = Math.round(message.addressId);
    }
    if (message.currencyInfo !== undefined) {
      obj.currencyInfo = Currency.toJSON(message.currencyInfo);
    }
    if (message.entityType !== "") {
      obj.entityType = message.entityType;
    }
    if (message.entityID !== "") {
      obj.entityID = message.entityID;
    }
    return obj;
  },

  create(base?: DeepPartial<BusinessRule>): BusinessRule {
    return BusinessRule.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BusinessRule>): BusinessRule {
    const message = createBaseBusinessRule();
    message.id = object.id ?? 0;
    message.tenantId = object.tenantId ?? 0;
    message.currency = object.currency ?? "";
    message.walletId = object.walletId ?? 0;
    message.ruleKey = object.ruleKey ?? "";
    message.ruleValue = object.ruleValue ?? "";
    message.ruleGroup = object.ruleGroup ?? "";
    message.ruleDescription = object.ruleDescription ?? "";
    message.ruleValidation = object.ruleValidation ?? "";
    message.addressId = object.addressId ?? 0;
    message.currencyInfo = (object.currencyInfo !== undefined && object.currencyInfo !== null)
      ? Currency.fromPartial(object.currencyInfo)
      : undefined;
    message.entityType = object.entityType ?? "";
    message.entityID = object.entityID ?? "";
    return message;
  },
};

function createBaseRules(): Rules {
  return {
    rulesContainer: "",
    rulesSignatures: [],
    locked: false,
    creationDate: undefined,
    updateDate: undefined,
    trails: [],
  };
}

export const Rules = {
  encode(message: Rules, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rulesContainer !== "") {
      writer.uint32(10).string(message.rulesContainer);
    }
    for (const v of message.rulesSignatures) {
      RuleUserSignature.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.locked !== false) {
      writer.uint32(24).bool(message.locked);
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(34).fork()).ldelim();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.trails) {
      Rules_Trail.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Rules {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.rulesContainer = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.rulesSignatures.push(RuleUserSignature.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.locked = reader.bool();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.trails.push(Rules_Trail.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Rules {
    return {
      rulesContainer: isSet(object.rulesContainer) ? globalThis.String(object.rulesContainer) : "",
      rulesSignatures: globalThis.Array.isArray(object?.rulesSignatures)
        ? object.rulesSignatures.map((e: any) => RuleUserSignature.fromJSON(e))
        : [],
      locked: isSet(object.locked) ? globalThis.Boolean(object.locked) : false,
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      updateDate: isSet(object.updateDate) ? fromJsonTimestamp(object.updateDate) : undefined,
      trails: globalThis.Array.isArray(object?.trails) ? object.trails.map((e: any) => Rules_Trail.fromJSON(e)) : [],
    };
  },

  toJSON(message: Rules): unknown {
    const obj: any = {};
    if (message.rulesContainer !== "") {
      obj.rulesContainer = message.rulesContainer;
    }
    if (message.rulesSignatures?.length) {
      obj.rulesSignatures = message.rulesSignatures.map((e) => RuleUserSignature.toJSON(e));
    }
    if (message.locked !== false) {
      obj.locked = message.locked;
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.updateDate !== undefined) {
      obj.updateDate = message.updateDate.toISOString();
    }
    if (message.trails?.length) {
      obj.trails = message.trails.map((e) => Rules_Trail.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Rules>): Rules {
    return Rules.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Rules>): Rules {
    const message = createBaseRules();
    message.rulesContainer = object.rulesContainer ?? "";
    message.rulesSignatures = object.rulesSignatures?.map((e) => RuleUserSignature.fromPartial(e)) || [];
    message.locked = object.locked ?? false;
    message.creationDate = object.creationDate ?? undefined;
    message.updateDate = object.updateDate ?? undefined;
    message.trails = object.trails?.map((e) => Rules_Trail.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRules_Trail(): Rules_Trail {
  return { id: 0, userId: 0, externalUserId: "", action: "", comment: "", date: undefined };
}

export const Rules_Trail = {
  encode(message: Rules_Trail, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.userId !== 0) {
      writer.uint32(16).uint64(message.userId);
    }
    if (message.externalUserId !== "") {
      writer.uint32(26).string(message.externalUserId);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    if (message.comment !== "") {
      writer.uint32(42).string(message.comment);
    }
    if (message.date !== undefined) {
      Timestamp.encode(toTimestamp(message.date), writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Rules_Trail {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRules_Trail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.userId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.externalUserId = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.comment = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.date = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Rules_Trail {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      externalUserId: isSet(object.externalUserId) ? globalThis.String(object.externalUserId) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      date: isSet(object.date) ? fromJsonTimestamp(object.date) : undefined,
    };
  },

  toJSON(message: Rules_Trail): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.externalUserId !== "") {
      obj.externalUserId = message.externalUserId;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.date !== undefined) {
      obj.date = message.date.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Rules_Trail>): Rules_Trail {
    return Rules_Trail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Rules_Trail>): Rules_Trail {
    const message = createBaseRules_Trail();
    message.id = object.id ?? 0;
    message.userId = object.userId ?? 0;
    message.externalUserId = object.externalUserId ?? "";
    message.action = object.action ?? "";
    message.comment = object.comment ?? "";
    message.date = object.date ?? undefined;
    return message;
  },
};

function createBaseRuleUserSignature(): RuleUserSignature {
  return { userId: "", signature: "" };
}

export const RuleUserSignature = {
  encode(message: RuleUserSignature, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.signature !== "") {
      writer.uint32(18).string(message.signature);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RuleUserSignature {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuleUserSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signature = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuleUserSignature {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      signature: isSet(object.signature) ? globalThis.String(object.signature) : "",
    };
  },

  toJSON(message: RuleUserSignature): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.signature !== "") {
      obj.signature = message.signature;
    }
    return obj;
  },

  create(base?: DeepPartial<RuleUserSignature>): RuleUserSignature {
    return RuleUserSignature.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuleUserSignature>): RuleUserSignature {
    const message = createBaseRuleUserSignature();
    message.userId = object.userId ?? "";
    message.signature = object.signature ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
