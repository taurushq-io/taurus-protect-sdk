// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: tag-service.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Empty } from "./google/protobuf/empty";
import { Tag } from "./tag";

export const protobufPackage = "tgvalidatord";

export interface GetTagsRequest {
  ids: string[];
  query: string;
}

export interface GetTagsReply {
  result: Tag[];
}

export interface CreateTagRequest {
  value: string;
  color: string;
}

export interface CreateTagReply {
  result: Tag | undefined;
}

export interface DeleteTagRequest {
  id: string;
}

function createBaseGetTagsRequest(): GetTagsRequest {
  return { ids: [], query: "" };
}

export const GetTagsRequest = {
  encode(message: GetTagsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.ids) {
      writer.uint32(10).string(v!);
    }
    if (message.query !== "") {
      writer.uint32(18).string(message.query);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetTagsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTagsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.query = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTagsRequest {
    return {
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.String(e)) : [],
      query: isSet(object.query) ? globalThis.String(object.query) : "",
    };
  },

  toJSON(message: GetTagsRequest): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    return obj;
  },

  create(base?: DeepPartial<GetTagsRequest>): GetTagsRequest {
    return GetTagsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetTagsRequest>): GetTagsRequest {
    const message = createBaseGetTagsRequest();
    message.ids = object.ids?.map((e) => e) || [];
    message.query = object.query ?? "";
    return message;
  },
};

function createBaseGetTagsReply(): GetTagsReply {
  return { result: [] };
}

export const GetTagsReply = {
  encode(message: GetTagsReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      Tag.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetTagsReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTagsReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(Tag.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTagsReply {
    return { result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => Tag.fromJSON(e)) : [] };
  },

  toJSON(message: GetTagsReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => Tag.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetTagsReply>): GetTagsReply {
    return GetTagsReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetTagsReply>): GetTagsReply {
    const message = createBaseGetTagsReply();
    message.result = object.result?.map((e) => Tag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateTagRequest(): CreateTagRequest {
  return { value: "", color: "" };
}

export const CreateTagRequest = {
  encode(message: CreateTagRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.color !== "") {
      writer.uint32(18).string(message.color);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateTagRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTagRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.color = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTagRequest {
    return {
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      color: isSet(object.color) ? globalThis.String(object.color) : "",
    };
  },

  toJSON(message: CreateTagRequest): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.color !== "") {
      obj.color = message.color;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateTagRequest>): CreateTagRequest {
    return CreateTagRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateTagRequest>): CreateTagRequest {
    const message = createBaseCreateTagRequest();
    message.value = object.value ?? "";
    message.color = object.color ?? "";
    return message;
  },
};

function createBaseCreateTagReply(): CreateTagReply {
  return { result: undefined };
}

export const CreateTagReply = {
  encode(message: CreateTagReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.result !== undefined) {
      Tag.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateTagReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTagReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result = Tag.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTagReply {
    return { result: isSet(object.result) ? Tag.fromJSON(object.result) : undefined };
  },

  toJSON(message: CreateTagReply): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = Tag.toJSON(message.result);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateTagReply>): CreateTagReply {
    return CreateTagReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateTagReply>): CreateTagReply {
    const message = createBaseCreateTagReply();
    message.result = (object.result !== undefined && object.result !== null)
      ? Tag.fromPartial(object.result)
      : undefined;
    return message;
  },
};

function createBaseDeleteTagRequest(): DeleteTagRequest {
  return { id: "" };
}

export const DeleteTagRequest = {
  encode(message: DeleteTagRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeleteTagRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteTagRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteTagRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: DeleteTagRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<DeleteTagRequest>): DeleteTagRequest {
    return DeleteTagRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteTagRequest>): DeleteTagRequest {
    const message = createBaseDeleteTagRequest();
    message.id = object.id ?? "";
    return message;
  },
};

export interface TagService {
  GetTags(request: GetTagsRequest): Promise<GetTagsReply>;
  CreateTag(request: CreateTagRequest): Promise<CreateTagReply>;
  DeleteTag(request: DeleteTagRequest): Promise<Empty>;
}

export const TagServiceServiceName = "tgvalidatord.TagService";
export class TagServiceClientImpl implements TagService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || TagServiceServiceName;
    this.rpc = rpc;
    this.GetTags = this.GetTags.bind(this);
    this.CreateTag = this.CreateTag.bind(this);
    this.DeleteTag = this.DeleteTag.bind(this);
  }
  GetTags(request: GetTagsRequest): Promise<GetTagsReply> {
    const data = GetTagsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetTags", data);
    return promise.then((data) => GetTagsReply.decode(_m0.Reader.create(data)));
  }

  CreateTag(request: CreateTagRequest): Promise<CreateTagReply> {
    const data = CreateTagRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateTag", data);
    return promise.then((data) => CreateTagReply.decode(_m0.Reader.create(data)));
  }

  DeleteTag(request: DeleteTagRequest): Promise<Empty> {
    const data = DeleteTagRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "DeleteTag", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
