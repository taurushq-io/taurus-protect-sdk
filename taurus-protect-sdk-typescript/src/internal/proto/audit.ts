// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: audit.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";
import { StringValue, UInt64Value } from "./google/protobuf/wrappers";

export const protobufPackage = "tgvalidatord";

export interface UserInfo {
  id: number | undefined;
  externalUserId: string | undefined;
  email: string | undefined;
  deleted: boolean;
}

export interface AuditTrail {
  id: string;
  user: UserInfo | undefined;
  entity: string;
  action: string;
  subAction: string | undefined;
  details: string | undefined;
  creationDate: Date | undefined;
}

function createBaseUserInfo(): UserInfo {
  return { id: undefined, externalUserId: undefined, email: undefined, deleted: false };
}

export const UserInfo = {
  encode(message: UserInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== undefined) {
      UInt64Value.encode({ value: message.id! }, writer.uint32(10).fork()).ldelim();
    }
    if (message.externalUserId !== undefined) {
      StringValue.encode({ value: message.externalUserId! }, writer.uint32(18).fork()).ldelim();
    }
    if (message.email !== undefined) {
      StringValue.encode({ value: message.email! }, writer.uint32(26).fork()).ldelim();
    }
    if (message.deleted !== false) {
      writer.uint32(32).bool(message.deleted);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UserInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.externalUserId = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.email = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.deleted = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserInfo {
    return {
      id: isSet(object.id) ? Number(object.id) : undefined,
      externalUserId: isSet(object.externalUserId) ? String(object.externalUserId) : undefined,
      email: isSet(object.email) ? String(object.email) : undefined,
      deleted: isSet(object.deleted) ? globalThis.Boolean(object.deleted) : false,
    };
  },

  toJSON(message: UserInfo): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.externalUserId !== undefined) {
      obj.externalUserId = message.externalUserId;
    }
    if (message.email !== undefined) {
      obj.email = message.email;
    }
    if (message.deleted !== false) {
      obj.deleted = message.deleted;
    }
    return obj;
  },

  create(base?: DeepPartial<UserInfo>): UserInfo {
    return UserInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserInfo>): UserInfo {
    const message = createBaseUserInfo();
    message.id = object.id ?? undefined;
    message.externalUserId = object.externalUserId ?? undefined;
    message.email = object.email ?? undefined;
    message.deleted = object.deleted ?? false;
    return message;
  },
};

function createBaseAuditTrail(): AuditTrail {
  return {
    id: "",
    user: undefined,
    entity: "",
    action: "",
    subAction: undefined,
    details: undefined,
    creationDate: undefined,
  };
}

export const AuditTrail = {
  encode(message: AuditTrail, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.user !== undefined) {
      UserInfo.encode(message.user, writer.uint32(18).fork()).ldelim();
    }
    if (message.entity !== "") {
      writer.uint32(26).string(message.entity);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    if (message.subAction !== undefined) {
      StringValue.encode({ value: message.subAction! }, writer.uint32(42).fork()).ldelim();
    }
    if (message.details !== undefined) {
      StringValue.encode({ value: message.details! }, writer.uint32(50).fork()).ldelim();
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AuditTrail {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuditTrail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.user = UserInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.entity = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.subAction = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.details = StringValue.decode(reader, reader.uint32()).value;
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuditTrail {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      user: isSet(object.user) ? UserInfo.fromJSON(object.user) : undefined,
      entity: isSet(object.entity) ? globalThis.String(object.entity) : "",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      subAction: isSet(object.subAction) ? String(object.subAction) : undefined,
      details: isSet(object.details) ? String(object.details) : undefined,
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
    };
  },

  toJSON(message: AuditTrail): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.user !== undefined) {
      obj.user = UserInfo.toJSON(message.user);
    }
    if (message.entity !== "") {
      obj.entity = message.entity;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.subAction !== undefined) {
      obj.subAction = message.subAction;
    }
    if (message.details !== undefined) {
      obj.details = message.details;
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<AuditTrail>): AuditTrail {
    return AuditTrail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuditTrail>): AuditTrail {
    const message = createBaseAuditTrail();
    message.id = object.id ?? "";
    message.user = (object.user !== undefined && object.user !== null) ? UserInfo.fromPartial(object.user) : undefined;
    message.entity = object.entity ?? "";
    message.action = object.action ?? "";
    message.subAction = object.subAction ?? undefined;
    message.details = object.details ?? undefined;
    message.creationDate = object.creationDate ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
