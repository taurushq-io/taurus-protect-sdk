// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: change.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface Change {
  id: number;
  tenantId: number;
  creatorId: number;
  action: string;
  entityId: number;
  entity: string;
  changes: { [key: string]: string };
  creationDate: Date | undefined;
  comment: string;
  entityUUID: string;
  /** The externalUserID of the user that created the change. */
  creatorExternalId: string;
}

export enum Change_Status {
  Undefined = 0,
  Created = 1,
  Approved = 2,
  Rejected = 3,
  Canceled = 4,
  UNRECOGNIZED = -1,
}

export function change_StatusFromJSON(object: any): Change_Status {
  switch (object) {
    case 0:
    case "Undefined":
      return Change_Status.Undefined;
    case 1:
    case "Created":
      return Change_Status.Created;
    case 2:
    case "Approved":
      return Change_Status.Approved;
    case 3:
    case "Rejected":
      return Change_Status.Rejected;
    case 4:
    case "Canceled":
      return Change_Status.Canceled;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Change_Status.UNRECOGNIZED;
  }
}

export function change_StatusToJSON(object: Change_Status): string {
  switch (object) {
    case Change_Status.Undefined:
      return "Undefined";
    case Change_Status.Created:
      return "Created";
    case Change_Status.Approved:
      return "Approved";
    case Change_Status.Rejected:
      return "Rejected";
    case Change_Status.Canceled:
      return "Canceled";
    case Change_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Change_ChangesEntry {
  key: string;
  value: string;
}

function createBaseChange(): Change {
  return {
    id: 0,
    tenantId: 0,
    creatorId: 0,
    action: "",
    entityId: 0,
    entity: "",
    changes: {},
    creationDate: undefined,
    comment: "",
    entityUUID: "",
    creatorExternalId: "",
  };
}

export const Change = {
  encode(message: Change, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.tenantId !== 0) {
      writer.uint32(16).uint64(message.tenantId);
    }
    if (message.creatorId !== 0) {
      writer.uint32(24).uint64(message.creatorId);
    }
    if (message.action !== "") {
      writer.uint32(34).string(message.action);
    }
    if (message.entityId !== 0) {
      writer.uint32(40).uint64(message.entityId);
    }
    if (message.entity !== "") {
      writer.uint32(50).string(message.entity);
    }
    Object.entries(message.changes).forEach(([key, value]) => {
      Change_ChangesEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).ldelim();
    });
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(66).fork()).ldelim();
    }
    if (message.comment !== "") {
      writer.uint32(74).string(message.comment);
    }
    if (message.entityUUID !== "") {
      writer.uint32(82).string(message.entityUUID);
    }
    if (message.creatorExternalId !== "") {
      writer.uint32(90).string(message.creatorExternalId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Change {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.creatorId = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.action = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.entityId = longToNumber(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.entity = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          const entry7 = Change_ChangesEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.changes[entry7.key] = entry7.value;
          }
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.comment = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.entityUUID = reader.string();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.creatorExternalId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Change {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      creatorId: isSet(object.creatorId) ? globalThis.Number(object.creatorId) : 0,
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      entityId: isSet(object.entityId) ? globalThis.Number(object.entityId) : 0,
      entity: isSet(object.entity) ? globalThis.String(object.entity) : "",
      changes: isObject(object.changes)
        ? Object.entries(object.changes).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      entityUUID: isSet(object.entityUUID) ? globalThis.String(object.entityUUID) : "",
      creatorExternalId: isSet(object.creatorExternalId) ? globalThis.String(object.creatorExternalId) : "",
    };
  },

  toJSON(message: Change): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.creatorId !== 0) {
      obj.creatorId = Math.round(message.creatorId);
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.entityId !== 0) {
      obj.entityId = Math.round(message.entityId);
    }
    if (message.entity !== "") {
      obj.entity = message.entity;
    }
    if (message.changes) {
      const entries = Object.entries(message.changes);
      if (entries.length > 0) {
        obj.changes = {};
        entries.forEach(([k, v]) => {
          obj.changes[k] = v;
        });
      }
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.entityUUID !== "") {
      obj.entityUUID = message.entityUUID;
    }
    if (message.creatorExternalId !== "") {
      obj.creatorExternalId = message.creatorExternalId;
    }
    return obj;
  },

  create(base?: DeepPartial<Change>): Change {
    return Change.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Change>): Change {
    const message = createBaseChange();
    message.id = object.id ?? 0;
    message.tenantId = object.tenantId ?? 0;
    message.creatorId = object.creatorId ?? 0;
    message.action = object.action ?? "";
    message.entityId = object.entityId ?? 0;
    message.entity = object.entity ?? "";
    message.changes = Object.entries(object.changes ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.creationDate = object.creationDate ?? undefined;
    message.comment = object.comment ?? "";
    message.entityUUID = object.entityUUID ?? "";
    message.creatorExternalId = object.creatorExternalId ?? "";
    return message;
  },
};

function createBaseChange_ChangesEntry(): Change_ChangesEntry {
  return { key: "", value: "" };
}

export const Change_ChangesEntry = {
  encode(message: Change_ChangesEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Change_ChangesEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChange_ChangesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Change_ChangesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Change_ChangesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Change_ChangesEntry>): Change_ChangesEntry {
    return Change_ChangesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Change_ChangesEntry>): Change_ChangesEntry {
    const message = createBaseChange_ChangesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
