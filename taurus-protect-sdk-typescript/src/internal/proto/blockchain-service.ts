// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: blockchain-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Currency } from "./currency";
import { Empty } from "./google/protobuf/empty";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface GetBlockchainsInfoReply {
  dot:
    | DOTBlockchainInfo
    | undefined;
  /** @deprecated */
  eth: EVMBlockchainInfo | undefined;
  evm: EVMBlockchainInfo | undefined;
}

export interface DOTBlockchainInfo {
  currentEra: number;
  maxNominations: number;
  forkNumber: number;
  forkMigratedAt: Date | undefined;
}

export interface EVMBlockchainInfo {
  chainId: number;
}

export interface XTZBlockchainInfo {
  currentCycle: number;
}

export interface GetBlockchainsRequest {
  blockchain: string;
  network: string;
  includeBlockHeight: boolean;
}

export interface GetBlockchainsReply {
  blockchains: BlockchainEntity[];
}

export interface BlockchainEntity {
  symbol: string;
  name: string;
  baseCurrency: Currency | undefined;
  dotInfo:
    | DOTBlockchainInfo
    | undefined;
  /** @deprecated */
  ethInfo: EVMBlockchainInfo | undefined;
  network: string;
  chainId: string;
  blackholeAddress: string;
  confirmations: number;
  blockHeight: number;
  isLayer2Chain: boolean;
  layer1Network: string;
  xtzInfo: XTZBlockchainInfo | undefined;
}

function createBaseGetBlockchainsInfoReply(): GetBlockchainsInfoReply {
  return { dot: undefined, eth: undefined, evm: undefined };
}

export const GetBlockchainsInfoReply = {
  encode(message: GetBlockchainsInfoReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.dot !== undefined) {
      DOTBlockchainInfo.encode(message.dot, writer.uint32(10).fork()).ldelim();
    }
    if (message.eth !== undefined) {
      EVMBlockchainInfo.encode(message.eth, writer.uint32(18).fork()).ldelim();
    }
    if (message.evm !== undefined) {
      EVMBlockchainInfo.encode(message.evm, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBlockchainsInfoReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBlockchainsInfoReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.dot = DOTBlockchainInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.eth = EVMBlockchainInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.evm = EVMBlockchainInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBlockchainsInfoReply {
    return {
      dot: isSet(object.dot) ? DOTBlockchainInfo.fromJSON(object.dot) : undefined,
      eth: isSet(object.eth) ? EVMBlockchainInfo.fromJSON(object.eth) : undefined,
      evm: isSet(object.evm) ? EVMBlockchainInfo.fromJSON(object.evm) : undefined,
    };
  },

  toJSON(message: GetBlockchainsInfoReply): unknown {
    const obj: any = {};
    if (message.dot !== undefined) {
      obj.dot = DOTBlockchainInfo.toJSON(message.dot);
    }
    if (message.eth !== undefined) {
      obj.eth = EVMBlockchainInfo.toJSON(message.eth);
    }
    if (message.evm !== undefined) {
      obj.evm = EVMBlockchainInfo.toJSON(message.evm);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBlockchainsInfoReply>): GetBlockchainsInfoReply {
    return GetBlockchainsInfoReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBlockchainsInfoReply>): GetBlockchainsInfoReply {
    const message = createBaseGetBlockchainsInfoReply();
    message.dot = (object.dot !== undefined && object.dot !== null)
      ? DOTBlockchainInfo.fromPartial(object.dot)
      : undefined;
    message.eth = (object.eth !== undefined && object.eth !== null)
      ? EVMBlockchainInfo.fromPartial(object.eth)
      : undefined;
    message.evm = (object.evm !== undefined && object.evm !== null)
      ? EVMBlockchainInfo.fromPartial(object.evm)
      : undefined;
    return message;
  },
};

function createBaseDOTBlockchainInfo(): DOTBlockchainInfo {
  return { currentEra: 0, maxNominations: 0, forkNumber: 0, forkMigratedAt: undefined };
}

export const DOTBlockchainInfo = {
  encode(message: DOTBlockchainInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.currentEra !== 0) {
      writer.uint32(8).uint64(message.currentEra);
    }
    if (message.maxNominations !== 0) {
      writer.uint32(16).uint64(message.maxNominations);
    }
    if (message.forkNumber !== 0) {
      writer.uint32(24).uint64(message.forkNumber);
    }
    if (message.forkMigratedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.forkMigratedAt), writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DOTBlockchainInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDOTBlockchainInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.currentEra = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxNominations = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.forkNumber = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.forkMigratedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DOTBlockchainInfo {
    return {
      currentEra: isSet(object.currentEra) ? globalThis.Number(object.currentEra) : 0,
      maxNominations: isSet(object.maxNominations) ? globalThis.Number(object.maxNominations) : 0,
      forkNumber: isSet(object.forkNumber) ? globalThis.Number(object.forkNumber) : 0,
      forkMigratedAt: isSet(object.forkMigratedAt) ? fromJsonTimestamp(object.forkMigratedAt) : undefined,
    };
  },

  toJSON(message: DOTBlockchainInfo): unknown {
    const obj: any = {};
    if (message.currentEra !== 0) {
      obj.currentEra = Math.round(message.currentEra);
    }
    if (message.maxNominations !== 0) {
      obj.maxNominations = Math.round(message.maxNominations);
    }
    if (message.forkNumber !== 0) {
      obj.forkNumber = Math.round(message.forkNumber);
    }
    if (message.forkMigratedAt !== undefined) {
      obj.forkMigratedAt = message.forkMigratedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<DOTBlockchainInfo>): DOTBlockchainInfo {
    return DOTBlockchainInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DOTBlockchainInfo>): DOTBlockchainInfo {
    const message = createBaseDOTBlockchainInfo();
    message.currentEra = object.currentEra ?? 0;
    message.maxNominations = object.maxNominations ?? 0;
    message.forkNumber = object.forkNumber ?? 0;
    message.forkMigratedAt = object.forkMigratedAt ?? undefined;
    return message;
  },
};

function createBaseEVMBlockchainInfo(): EVMBlockchainInfo {
  return { chainId: 0 };
}

export const EVMBlockchainInfo = {
  encode(message: EVMBlockchainInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.chainId !== 0) {
      writer.uint32(8).uint64(message.chainId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EVMBlockchainInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEVMBlockchainInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.chainId = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EVMBlockchainInfo {
    return { chainId: isSet(object.chainId) ? globalThis.Number(object.chainId) : 0 };
  },

  toJSON(message: EVMBlockchainInfo): unknown {
    const obj: any = {};
    if (message.chainId !== 0) {
      obj.chainId = Math.round(message.chainId);
    }
    return obj;
  },

  create(base?: DeepPartial<EVMBlockchainInfo>): EVMBlockchainInfo {
    return EVMBlockchainInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EVMBlockchainInfo>): EVMBlockchainInfo {
    const message = createBaseEVMBlockchainInfo();
    message.chainId = object.chainId ?? 0;
    return message;
  },
};

function createBaseXTZBlockchainInfo(): XTZBlockchainInfo {
  return { currentCycle: 0 };
}

export const XTZBlockchainInfo = {
  encode(message: XTZBlockchainInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.currentCycle !== 0) {
      writer.uint32(8).uint64(message.currentCycle);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): XTZBlockchainInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseXTZBlockchainInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.currentCycle = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): XTZBlockchainInfo {
    return { currentCycle: isSet(object.currentCycle) ? globalThis.Number(object.currentCycle) : 0 };
  },

  toJSON(message: XTZBlockchainInfo): unknown {
    const obj: any = {};
    if (message.currentCycle !== 0) {
      obj.currentCycle = Math.round(message.currentCycle);
    }
    return obj;
  },

  create(base?: DeepPartial<XTZBlockchainInfo>): XTZBlockchainInfo {
    return XTZBlockchainInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<XTZBlockchainInfo>): XTZBlockchainInfo {
    const message = createBaseXTZBlockchainInfo();
    message.currentCycle = object.currentCycle ?? 0;
    return message;
  },
};

function createBaseGetBlockchainsRequest(): GetBlockchainsRequest {
  return { blockchain: "", network: "", includeBlockHeight: false };
}

export const GetBlockchainsRequest = {
  encode(message: GetBlockchainsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.blockchain !== "") {
      writer.uint32(10).string(message.blockchain);
    }
    if (message.network !== "") {
      writer.uint32(18).string(message.network);
    }
    if (message.includeBlockHeight !== false) {
      writer.uint32(24).bool(message.includeBlockHeight);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBlockchainsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBlockchainsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.network = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.includeBlockHeight = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBlockchainsRequest {
    return {
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      network: isSet(object.network) ? globalThis.String(object.network) : "",
      includeBlockHeight: isSet(object.includeBlockHeight) ? globalThis.Boolean(object.includeBlockHeight) : false,
    };
  },

  toJSON(message: GetBlockchainsRequest): unknown {
    const obj: any = {};
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    if (message.includeBlockHeight !== false) {
      obj.includeBlockHeight = message.includeBlockHeight;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBlockchainsRequest>): GetBlockchainsRequest {
    return GetBlockchainsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBlockchainsRequest>): GetBlockchainsRequest {
    const message = createBaseGetBlockchainsRequest();
    message.blockchain = object.blockchain ?? "";
    message.network = object.network ?? "";
    message.includeBlockHeight = object.includeBlockHeight ?? false;
    return message;
  },
};

function createBaseGetBlockchainsReply(): GetBlockchainsReply {
  return { blockchains: [] };
}

export const GetBlockchainsReply = {
  encode(message: GetBlockchainsReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.blockchains) {
      BlockchainEntity.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetBlockchainsReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBlockchainsReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockchains.push(BlockchainEntity.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBlockchainsReply {
    return {
      blockchains: globalThis.Array.isArray(object?.blockchains)
        ? object.blockchains.map((e: any) => BlockchainEntity.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetBlockchainsReply): unknown {
    const obj: any = {};
    if (message.blockchains?.length) {
      obj.blockchains = message.blockchains.map((e) => BlockchainEntity.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetBlockchainsReply>): GetBlockchainsReply {
    return GetBlockchainsReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBlockchainsReply>): GetBlockchainsReply {
    const message = createBaseGetBlockchainsReply();
    message.blockchains = object.blockchains?.map((e) => BlockchainEntity.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBlockchainEntity(): BlockchainEntity {
  return {
    symbol: "",
    name: "",
    baseCurrency: undefined,
    dotInfo: undefined,
    ethInfo: undefined,
    network: "",
    chainId: "",
    blackholeAddress: "",
    confirmations: 0,
    blockHeight: 0,
    isLayer2Chain: false,
    layer1Network: "",
    xtzInfo: undefined,
  };
}

export const BlockchainEntity = {
  encode(message: BlockchainEntity, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.baseCurrency !== undefined) {
      Currency.encode(message.baseCurrency, writer.uint32(26).fork()).ldelim();
    }
    if (message.dotInfo !== undefined) {
      DOTBlockchainInfo.encode(message.dotInfo, writer.uint32(34).fork()).ldelim();
    }
    if (message.ethInfo !== undefined) {
      EVMBlockchainInfo.encode(message.ethInfo, writer.uint32(42).fork()).ldelim();
    }
    if (message.network !== "") {
      writer.uint32(50).string(message.network);
    }
    if (message.chainId !== "") {
      writer.uint32(58).string(message.chainId);
    }
    if (message.blackholeAddress !== "") {
      writer.uint32(66).string(message.blackholeAddress);
    }
    if (message.confirmations !== 0) {
      writer.uint32(72).uint64(message.confirmations);
    }
    if (message.blockHeight !== 0) {
      writer.uint32(80).uint64(message.blockHeight);
    }
    if (message.isLayer2Chain !== false) {
      writer.uint32(88).bool(message.isLayer2Chain);
    }
    if (message.layer1Network !== "") {
      writer.uint32(98).string(message.layer1Network);
    }
    if (message.xtzInfo !== undefined) {
      XTZBlockchainInfo.encode(message.xtzInfo, writer.uint32(106).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BlockchainEntity {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockchainEntity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.baseCurrency = Currency.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.dotInfo = DOTBlockchainInfo.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.ethInfo = EVMBlockchainInfo.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.network = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.chainId = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.blackholeAddress = reader.string();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.confirmations = longToNumber(reader.uint64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.blockHeight = longToNumber(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.isLayer2Chain = reader.bool();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.layer1Network = reader.string();
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.xtzInfo = XTZBlockchainInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockchainEntity {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      baseCurrency: isSet(object.baseCurrency) ? Currency.fromJSON(object.baseCurrency) : undefined,
      dotInfo: isSet(object.dotInfo) ? DOTBlockchainInfo.fromJSON(object.dotInfo) : undefined,
      ethInfo: isSet(object.ethInfo) ? EVMBlockchainInfo.fromJSON(object.ethInfo) : undefined,
      network: isSet(object.network) ? globalThis.String(object.network) : "",
      chainId: isSet(object.chainId) ? globalThis.String(object.chainId) : "",
      blackholeAddress: isSet(object.blackholeAddress) ? globalThis.String(object.blackholeAddress) : "",
      confirmations: isSet(object.confirmations) ? globalThis.Number(object.confirmations) : 0,
      blockHeight: isSet(object.blockHeight) ? globalThis.Number(object.blockHeight) : 0,
      isLayer2Chain: isSet(object.isLayer2Chain) ? globalThis.Boolean(object.isLayer2Chain) : false,
      layer1Network: isSet(object.layer1Network) ? globalThis.String(object.layer1Network) : "",
      xtzInfo: isSet(object.xtzInfo) ? XTZBlockchainInfo.fromJSON(object.xtzInfo) : undefined,
    };
  },

  toJSON(message: BlockchainEntity): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.baseCurrency !== undefined) {
      obj.baseCurrency = Currency.toJSON(message.baseCurrency);
    }
    if (message.dotInfo !== undefined) {
      obj.dotInfo = DOTBlockchainInfo.toJSON(message.dotInfo);
    }
    if (message.ethInfo !== undefined) {
      obj.ethInfo = EVMBlockchainInfo.toJSON(message.ethInfo);
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.blackholeAddress !== "") {
      obj.blackholeAddress = message.blackholeAddress;
    }
    if (message.confirmations !== 0) {
      obj.confirmations = Math.round(message.confirmations);
    }
    if (message.blockHeight !== 0) {
      obj.blockHeight = Math.round(message.blockHeight);
    }
    if (message.isLayer2Chain !== false) {
      obj.isLayer2Chain = message.isLayer2Chain;
    }
    if (message.layer1Network !== "") {
      obj.layer1Network = message.layer1Network;
    }
    if (message.xtzInfo !== undefined) {
      obj.xtzInfo = XTZBlockchainInfo.toJSON(message.xtzInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<BlockchainEntity>): BlockchainEntity {
    return BlockchainEntity.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BlockchainEntity>): BlockchainEntity {
    const message = createBaseBlockchainEntity();
    message.symbol = object.symbol ?? "";
    message.name = object.name ?? "";
    message.baseCurrency = (object.baseCurrency !== undefined && object.baseCurrency !== null)
      ? Currency.fromPartial(object.baseCurrency)
      : undefined;
    message.dotInfo = (object.dotInfo !== undefined && object.dotInfo !== null)
      ? DOTBlockchainInfo.fromPartial(object.dotInfo)
      : undefined;
    message.ethInfo = (object.ethInfo !== undefined && object.ethInfo !== null)
      ? EVMBlockchainInfo.fromPartial(object.ethInfo)
      : undefined;
    message.network = object.network ?? "";
    message.chainId = object.chainId ?? "";
    message.blackholeAddress = object.blackholeAddress ?? "";
    message.confirmations = object.confirmations ?? 0;
    message.blockHeight = object.blockHeight ?? 0;
    message.isLayer2Chain = object.isLayer2Chain ?? false;
    message.layer1Network = object.layer1Network ?? "";
    message.xtzInfo = (object.xtzInfo !== undefined && object.xtzInfo !== null)
      ? XTZBlockchainInfo.fromPartial(object.xtzInfo)
      : undefined;
    return message;
  },
};

export interface BlockchainService {
  GetBlockchains(request: GetBlockchainsRequest): Promise<GetBlockchainsReply>;
  /** @deprecated */
  GetBlockchainsInfo(request: Empty): Promise<GetBlockchainsInfoReply>;
}

export const BlockchainServiceServiceName = "tgvalidatord.BlockchainService";
export class BlockchainServiceClientImpl implements BlockchainService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || BlockchainServiceServiceName;
    this.rpc = rpc;
    this.GetBlockchains = this.GetBlockchains.bind(this);
    this.GetBlockchainsInfo = this.GetBlockchainsInfo.bind(this);
  }
  GetBlockchains(request: GetBlockchainsRequest): Promise<GetBlockchainsReply> {
    const data = GetBlockchainsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetBlockchains", data);
    return promise.then((data) => GetBlockchainsReply.decode(_m0.Reader.create(data)));
  }

  GetBlockchainsInfo(request: Empty): Promise<GetBlockchainsInfoReply> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetBlockchainsInfo", data);
    return promise.then((data) => GetBlockchainsInfoReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
