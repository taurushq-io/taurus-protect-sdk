// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: exchange.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Currency } from "./currency";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface Exchange {
  id: number;
  exchange: string;
  account: string;
  currency: string;
  type: string;
  totalBalance: string;
  status: string;
  container: string;
  label: string;
  creationDate: Date | undefined;
  updateDate: Date | undefined;
  currencyInfo: Currency | undefined;
  hasWLA: boolean;
  displayLabel: string;
  baseCurrencyValuation: string;
}

export interface ExchangeCounterparty {
  name: string;
  baseCurrencyValuation: string;
}

function createBaseExchange(): Exchange {
  return {
    id: 0,
    exchange: "",
    account: "",
    currency: "",
    type: "",
    totalBalance: "",
    status: "",
    container: "",
    label: "",
    creationDate: undefined,
    updateDate: undefined,
    currencyInfo: undefined,
    hasWLA: false,
    displayLabel: "",
    baseCurrencyValuation: "",
  };
}

export const Exchange = {
  encode(message: Exchange, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.exchange !== "") {
      writer.uint32(18).string(message.exchange);
    }
    if (message.account !== "") {
      writer.uint32(26).string(message.account);
    }
    if (message.currency !== "") {
      writer.uint32(34).string(message.currency);
    }
    if (message.type !== "") {
      writer.uint32(42).string(message.type);
    }
    if (message.totalBalance !== "") {
      writer.uint32(50).string(message.totalBalance);
    }
    if (message.status !== "") {
      writer.uint32(58).string(message.status);
    }
    if (message.container !== "") {
      writer.uint32(66).string(message.container);
    }
    if (message.label !== "") {
      writer.uint32(74).string(message.label);
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(82).fork()).ldelim();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(90).fork()).ldelim();
    }
    if (message.currencyInfo !== undefined) {
      Currency.encode(message.currencyInfo, writer.uint32(98).fork()).ldelim();
    }
    if (message.hasWLA !== false) {
      writer.uint32(104).bool(message.hasWLA);
    }
    if (message.displayLabel !== "") {
      writer.uint32(114).string(message.displayLabel);
    }
    if (message.baseCurrencyValuation !== "") {
      writer.uint32(122).string(message.baseCurrencyValuation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Exchange {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.exchange = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.account = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.currency = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.type = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.totalBalance = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.status = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.container = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.label = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.currencyInfo = Currency.decode(reader, reader.uint32());
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.hasWLA = reader.bool();
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.displayLabel = reader.string();
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.baseCurrencyValuation = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Exchange {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      exchange: isSet(object.exchange) ? globalThis.String(object.exchange) : "",
      account: isSet(object.account) ? globalThis.String(object.account) : "",
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      totalBalance: isSet(object.totalBalance) ? globalThis.String(object.totalBalance) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      container: isSet(object.container) ? globalThis.String(object.container) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      updateDate: isSet(object.updateDate) ? fromJsonTimestamp(object.updateDate) : undefined,
      currencyInfo: isSet(object.currencyInfo) ? Currency.fromJSON(object.currencyInfo) : undefined,
      hasWLA: isSet(object.hasWLA) ? globalThis.Boolean(object.hasWLA) : false,
      displayLabel: isSet(object.displayLabel) ? globalThis.String(object.displayLabel) : "",
      baseCurrencyValuation: isSet(object.baseCurrencyValuation) ? globalThis.String(object.baseCurrencyValuation) : "",
    };
  },

  toJSON(message: Exchange): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.exchange !== "") {
      obj.exchange = message.exchange;
    }
    if (message.account !== "") {
      obj.account = message.account;
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.totalBalance !== "") {
      obj.totalBalance = message.totalBalance;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.container !== "") {
      obj.container = message.container;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.updateDate !== undefined) {
      obj.updateDate = message.updateDate.toISOString();
    }
    if (message.currencyInfo !== undefined) {
      obj.currencyInfo = Currency.toJSON(message.currencyInfo);
    }
    if (message.hasWLA !== false) {
      obj.hasWLA = message.hasWLA;
    }
    if (message.displayLabel !== "") {
      obj.displayLabel = message.displayLabel;
    }
    if (message.baseCurrencyValuation !== "") {
      obj.baseCurrencyValuation = message.baseCurrencyValuation;
    }
    return obj;
  },

  create(base?: DeepPartial<Exchange>): Exchange {
    return Exchange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Exchange>): Exchange {
    const message = createBaseExchange();
    message.id = object.id ?? 0;
    message.exchange = object.exchange ?? "";
    message.account = object.account ?? "";
    message.currency = object.currency ?? "";
    message.type = object.type ?? "";
    message.totalBalance = object.totalBalance ?? "";
    message.status = object.status ?? "";
    message.container = object.container ?? "";
    message.label = object.label ?? "";
    message.creationDate = object.creationDate ?? undefined;
    message.updateDate = object.updateDate ?? undefined;
    message.currencyInfo = (object.currencyInfo !== undefined && object.currencyInfo !== null)
      ? Currency.fromPartial(object.currencyInfo)
      : undefined;
    message.hasWLA = object.hasWLA ?? false;
    message.displayLabel = object.displayLabel ?? "";
    message.baseCurrencyValuation = object.baseCurrencyValuation ?? "";
    return message;
  },
};

function createBaseExchangeCounterparty(): ExchangeCounterparty {
  return { name: "", baseCurrencyValuation: "" };
}

export const ExchangeCounterparty = {
  encode(message: ExchangeCounterparty, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.baseCurrencyValuation !== "") {
      writer.uint32(18).string(message.baseCurrencyValuation);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExchangeCounterparty {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExchangeCounterparty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.baseCurrencyValuation = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExchangeCounterparty {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      baseCurrencyValuation: isSet(object.baseCurrencyValuation) ? globalThis.String(object.baseCurrencyValuation) : "",
    };
  },

  toJSON(message: ExchangeCounterparty): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.baseCurrencyValuation !== "") {
      obj.baseCurrencyValuation = message.baseCurrencyValuation;
    }
    return obj;
  },

  create(base?: DeepPartial<ExchangeCounterparty>): ExchangeCounterparty {
    return ExchangeCounterparty.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExchangeCounterparty>): ExchangeCounterparty {
    const message = createBaseExchangeCounterparty();
    message.name = object.name ?? "";
    message.baseCurrencyValuation = object.baseCurrencyValuation ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
