// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: token-metadata.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

export const protobufPackage = "tgvalidatord";

export interface ERCTokenMetadata {
  name: string;
  description: string;
  decimals: number;
  dataType: string;
  base64Data: string;
  uri: string;
}

export interface FATokenMetadata {
  name: string;
  symbol: string;
  decimals: number;
  dataType: string;
  base64Data: string;
  uri: string;
}

export interface CryptoPunkMetadata {
  punkId: number;
  punkAttributes: string;
  image: Uint8Array;
}

function createBaseERCTokenMetadata(): ERCTokenMetadata {
  return { name: "", description: "", decimals: 0, dataType: "", base64Data: "", uri: "" };
}

export const ERCTokenMetadata = {
  encode(message: ERCTokenMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.decimals !== 0) {
      writer.uint32(24).uint64(message.decimals);
    }
    if (message.dataType !== "") {
      writer.uint32(34).string(message.dataType);
    }
    if (message.base64Data !== "") {
      writer.uint32(42).string(message.base64Data);
    }
    if (message.uri !== "") {
      writer.uint32(50).string(message.uri);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ERCTokenMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseERCTokenMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.decimals = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.dataType = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.base64Data = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.uri = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ERCTokenMetadata {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      decimals: isSet(object.decimals) ? globalThis.Number(object.decimals) : 0,
      dataType: isSet(object.dataType) ? globalThis.String(object.dataType) : "",
      base64Data: isSet(object.base64Data) ? globalThis.String(object.base64Data) : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
    };
  },

  toJSON(message: ERCTokenMetadata): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.decimals !== 0) {
      obj.decimals = Math.round(message.decimals);
    }
    if (message.dataType !== "") {
      obj.dataType = message.dataType;
    }
    if (message.base64Data !== "") {
      obj.base64Data = message.base64Data;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    return obj;
  },

  create(base?: DeepPartial<ERCTokenMetadata>): ERCTokenMetadata {
    return ERCTokenMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ERCTokenMetadata>): ERCTokenMetadata {
    const message = createBaseERCTokenMetadata();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.decimals = object.decimals ?? 0;
    message.dataType = object.dataType ?? "";
    message.base64Data = object.base64Data ?? "";
    message.uri = object.uri ?? "";
    return message;
  },
};

function createBaseFATokenMetadata(): FATokenMetadata {
  return { name: "", symbol: "", decimals: 0, dataType: "", base64Data: "", uri: "" };
}

export const FATokenMetadata = {
  encode(message: FATokenMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.decimals !== 0) {
      writer.uint32(24).uint64(message.decimals);
    }
    if (message.dataType !== "") {
      writer.uint32(34).string(message.dataType);
    }
    if (message.base64Data !== "") {
      writer.uint32(42).string(message.base64Data);
    }
    if (message.uri !== "") {
      writer.uint32(50).string(message.uri);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FATokenMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFATokenMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.decimals = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.dataType = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.base64Data = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.uri = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FATokenMetadata {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      decimals: isSet(object.decimals) ? globalThis.Number(object.decimals) : 0,
      dataType: isSet(object.dataType) ? globalThis.String(object.dataType) : "",
      base64Data: isSet(object.base64Data) ? globalThis.String(object.base64Data) : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
    };
  },

  toJSON(message: FATokenMetadata): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.decimals !== 0) {
      obj.decimals = Math.round(message.decimals);
    }
    if (message.dataType !== "") {
      obj.dataType = message.dataType;
    }
    if (message.base64Data !== "") {
      obj.base64Data = message.base64Data;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    return obj;
  },

  create(base?: DeepPartial<FATokenMetadata>): FATokenMetadata {
    return FATokenMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FATokenMetadata>): FATokenMetadata {
    const message = createBaseFATokenMetadata();
    message.name = object.name ?? "";
    message.symbol = object.symbol ?? "";
    message.decimals = object.decimals ?? 0;
    message.dataType = object.dataType ?? "";
    message.base64Data = object.base64Data ?? "";
    message.uri = object.uri ?? "";
    return message;
  },
};

function createBaseCryptoPunkMetadata(): CryptoPunkMetadata {
  return { punkId: 0, punkAttributes: "", image: new Uint8Array(0) };
}

export const CryptoPunkMetadata = {
  encode(message: CryptoPunkMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.punkId !== 0) {
      writer.uint32(8).uint64(message.punkId);
    }
    if (message.punkAttributes !== "") {
      writer.uint32(18).string(message.punkAttributes);
    }
    if (message.image.length !== 0) {
      writer.uint32(26).bytes(message.image);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CryptoPunkMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCryptoPunkMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.punkId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.punkAttributes = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.image = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CryptoPunkMetadata {
    return {
      punkId: isSet(object.punkId) ? globalThis.Number(object.punkId) : 0,
      punkAttributes: isSet(object.punkAttributes) ? globalThis.String(object.punkAttributes) : "",
      image: isSet(object.image) ? bytesFromBase64(object.image) : new Uint8Array(0),
    };
  },

  toJSON(message: CryptoPunkMetadata): unknown {
    const obj: any = {};
    if (message.punkId !== 0) {
      obj.punkId = Math.round(message.punkId);
    }
    if (message.punkAttributes !== "") {
      obj.punkAttributes = message.punkAttributes;
    }
    if (message.image.length !== 0) {
      obj.image = base64FromBytes(message.image);
    }
    return obj;
  },

  create(base?: DeepPartial<CryptoPunkMetadata>): CryptoPunkMetadata {
    return CryptoPunkMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CryptoPunkMetadata>): CryptoPunkMetadata {
    const message = createBaseCryptoPunkMetadata();
    message.punkId = object.punkId ?? 0;
    message.punkAttributes = object.punkAttributes ?? "";
    message.image = object.image ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
