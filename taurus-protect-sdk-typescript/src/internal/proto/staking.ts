// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: staking.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export enum StakeAccountType {
  StakeAccountTypeNone = 0,
  StakeAccountTypeSolana = 1,
  UNRECOGNIZED = -1,
}

export function stakeAccountTypeFromJSON(object: any): StakeAccountType {
  switch (object) {
    case 0:
    case "StakeAccountTypeNone":
      return StakeAccountType.StakeAccountTypeNone;
    case 1:
    case "StakeAccountTypeSolana":
      return StakeAccountType.StakeAccountTypeSolana;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StakeAccountType.UNRECOGNIZED;
  }
}

export function stakeAccountTypeToJSON(object: StakeAccountType): string {
  switch (object) {
    case StakeAccountType.StakeAccountTypeNone:
      return "StakeAccountTypeNone";
    case StakeAccountType.StakeAccountTypeSolana:
      return "StakeAccountTypeSolana";
    case StakeAccountType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SolanaStakeAccount {
  derivationIndex: number;
  state: SolanaStakeAccount_State;
  validatorAddress: string;
  activeBalance: number;
  inactiveBalance: number;
  allowMerge: boolean;
}

export enum SolanaStakeAccount_State {
  unknown = 0,
  inactive = 1,
  activating = 2,
  active = 3,
  deactivating = 4,
  UNRECOGNIZED = -1,
}

export function solanaStakeAccount_StateFromJSON(object: any): SolanaStakeAccount_State {
  switch (object) {
    case 0:
    case "unknown":
      return SolanaStakeAccount_State.unknown;
    case 1:
    case "inactive":
      return SolanaStakeAccount_State.inactive;
    case 2:
    case "activating":
      return SolanaStakeAccount_State.activating;
    case 3:
    case "active":
      return SolanaStakeAccount_State.active;
    case 4:
    case "deactivating":
      return SolanaStakeAccount_State.deactivating;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SolanaStakeAccount_State.UNRECOGNIZED;
  }
}

export function solanaStakeAccount_StateToJSON(object: SolanaStakeAccount_State): string {
  switch (object) {
    case SolanaStakeAccount_State.unknown:
      return "unknown";
    case SolanaStakeAccount_State.inactive:
      return "inactive";
    case SolanaStakeAccount_State.activating:
      return "activating";
    case SolanaStakeAccount_State.active:
      return "active";
    case SolanaStakeAccount_State.deactivating:
      return "deactivating";
    case SolanaStakeAccount_State.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface StakeAccount {
  id: string;
  addressId: number;
  accountAddress: string;
  createdAt: Date | undefined;
  updatedAt: Date | undefined;
  updatedAtBlock: number;
  accountType: StakeAccountType;
  solanaStakeAccount?: SolanaStakeAccount | undefined;
}

function createBaseSolanaStakeAccount(): SolanaStakeAccount {
  return {
    derivationIndex: 0,
    state: 0,
    validatorAddress: "",
    activeBalance: 0,
    inactiveBalance: 0,
    allowMerge: false,
  };
}

export const SolanaStakeAccount = {
  encode(message: SolanaStakeAccount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.derivationIndex !== 0) {
      writer.uint32(8).uint64(message.derivationIndex);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.validatorAddress !== "") {
      writer.uint32(26).string(message.validatorAddress);
    }
    if (message.activeBalance !== 0) {
      writer.uint32(32).uint64(message.activeBalance);
    }
    if (message.inactiveBalance !== 0) {
      writer.uint32(40).uint64(message.inactiveBalance);
    }
    if (message.allowMerge !== false) {
      writer.uint32(48).bool(message.allowMerge);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SolanaStakeAccount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolanaStakeAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.derivationIndex = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.validatorAddress = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.activeBalance = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.inactiveBalance = longToNumber(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.allowMerge = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolanaStakeAccount {
    return {
      derivationIndex: isSet(object.derivationIndex) ? globalThis.Number(object.derivationIndex) : 0,
      state: isSet(object.state) ? solanaStakeAccount_StateFromJSON(object.state) : 0,
      validatorAddress: isSet(object.validatorAddress) ? globalThis.String(object.validatorAddress) : "",
      activeBalance: isSet(object.activeBalance) ? globalThis.Number(object.activeBalance) : 0,
      inactiveBalance: isSet(object.inactiveBalance) ? globalThis.Number(object.inactiveBalance) : 0,
      allowMerge: isSet(object.allowMerge) ? globalThis.Boolean(object.allowMerge) : false,
    };
  },

  toJSON(message: SolanaStakeAccount): unknown {
    const obj: any = {};
    if (message.derivationIndex !== 0) {
      obj.derivationIndex = Math.round(message.derivationIndex);
    }
    if (message.state !== 0) {
      obj.state = solanaStakeAccount_StateToJSON(message.state);
    }
    if (message.validatorAddress !== "") {
      obj.validatorAddress = message.validatorAddress;
    }
    if (message.activeBalance !== 0) {
      obj.activeBalance = Math.round(message.activeBalance);
    }
    if (message.inactiveBalance !== 0) {
      obj.inactiveBalance = Math.round(message.inactiveBalance);
    }
    if (message.allowMerge !== false) {
      obj.allowMerge = message.allowMerge;
    }
    return obj;
  },

  create(base?: DeepPartial<SolanaStakeAccount>): SolanaStakeAccount {
    return SolanaStakeAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SolanaStakeAccount>): SolanaStakeAccount {
    const message = createBaseSolanaStakeAccount();
    message.derivationIndex = object.derivationIndex ?? 0;
    message.state = object.state ?? 0;
    message.validatorAddress = object.validatorAddress ?? "";
    message.activeBalance = object.activeBalance ?? 0;
    message.inactiveBalance = object.inactiveBalance ?? 0;
    message.allowMerge = object.allowMerge ?? false;
    return message;
  },
};

function createBaseStakeAccount(): StakeAccount {
  return {
    id: "",
    addressId: 0,
    accountAddress: "",
    createdAt: undefined,
    updatedAt: undefined,
    updatedAtBlock: 0,
    accountType: 0,
    solanaStakeAccount: undefined,
  };
}

export const StakeAccount = {
  encode(message: StakeAccount, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.addressId !== 0) {
      writer.uint32(16).uint64(message.addressId);
    }
    if (message.accountAddress !== "") {
      writer.uint32(26).string(message.accountAddress);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).ldelim();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).ldelim();
    }
    if (message.updatedAtBlock !== 0) {
      writer.uint32(48).uint64(message.updatedAtBlock);
    }
    if (message.accountType !== 0) {
      writer.uint32(56).int32(message.accountType);
    }
    if (message.solanaStakeAccount !== undefined) {
      SolanaStakeAccount.encode(message.solanaStakeAccount, writer.uint32(802).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StakeAccount {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStakeAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.addressId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.accountAddress = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.updatedAtBlock = longToNumber(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.accountType = reader.int32() as any;
          continue;
        case 100:
          if (tag !== 802) {
            break;
          }

          message.solanaStakeAccount = SolanaStakeAccount.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StakeAccount {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      addressId: isSet(object.addressId) ? globalThis.Number(object.addressId) : 0,
      accountAddress: isSet(object.accountAddress) ? globalThis.String(object.accountAddress) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      updatedAtBlock: isSet(object.updatedAtBlock) ? globalThis.Number(object.updatedAtBlock) : 0,
      accountType: isSet(object.accountType) ? stakeAccountTypeFromJSON(object.accountType) : 0,
      solanaStakeAccount: isSet(object.solanaStakeAccount)
        ? SolanaStakeAccount.fromJSON(object.solanaStakeAccount)
        : undefined,
    };
  },

  toJSON(message: StakeAccount): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.addressId !== 0) {
      obj.addressId = Math.round(message.addressId);
    }
    if (message.accountAddress !== "") {
      obj.accountAddress = message.accountAddress;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.updatedAtBlock !== 0) {
      obj.updatedAtBlock = Math.round(message.updatedAtBlock);
    }
    if (message.accountType !== 0) {
      obj.accountType = stakeAccountTypeToJSON(message.accountType);
    }
    if (message.solanaStakeAccount !== undefined) {
      obj.solanaStakeAccount = SolanaStakeAccount.toJSON(message.solanaStakeAccount);
    }
    return obj;
  },

  create(base?: DeepPartial<StakeAccount>): StakeAccount {
    return StakeAccount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StakeAccount>): StakeAccount {
    const message = createBaseStakeAccount();
    message.id = object.id ?? "";
    message.addressId = object.addressId ?? 0;
    message.accountAddress = object.accountAddress ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.updatedAtBlock = object.updatedAtBlock ?? 0;
    message.accountType = object.accountType ?? 0;
    message.solanaStakeAccount = (object.solanaStakeAccount !== undefined && object.solanaStakeAccount !== null)
      ? SolanaStakeAccount.fromPartial(object.solanaStakeAccount)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
