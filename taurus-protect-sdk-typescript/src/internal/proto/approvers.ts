// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: approvers.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";

export const protobufPackage = "tgvalidatord";

export interface ApproversGroup {
  externalGroupID: string;
  minimumSignatures: number;
}

export interface ParallelApproversGroups {
  sequential: ApproversGroup[];
}

export interface Approvers {
  parallel: ParallelApproversGroups[];
}

function createBaseApproversGroup(): ApproversGroup {
  return { externalGroupID: "", minimumSignatures: 0 };
}

export const ApproversGroup = {
  encode(message: ApproversGroup, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.externalGroupID !== "") {
      writer.uint32(10).string(message.externalGroupID);
    }
    if (message.minimumSignatures !== 0) {
      writer.uint32(16).uint32(message.minimumSignatures);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ApproversGroup {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproversGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.externalGroupID = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.minimumSignatures = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproversGroup {
    return {
      externalGroupID: isSet(object.externalGroupID) ? globalThis.String(object.externalGroupID) : "",
      minimumSignatures: isSet(object.minimumSignatures) ? globalThis.Number(object.minimumSignatures) : 0,
    };
  },

  toJSON(message: ApproversGroup): unknown {
    const obj: any = {};
    if (message.externalGroupID !== "") {
      obj.externalGroupID = message.externalGroupID;
    }
    if (message.minimumSignatures !== 0) {
      obj.minimumSignatures = Math.round(message.minimumSignatures);
    }
    return obj;
  },

  create(base?: DeepPartial<ApproversGroup>): ApproversGroup {
    return ApproversGroup.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApproversGroup>): ApproversGroup {
    const message = createBaseApproversGroup();
    message.externalGroupID = object.externalGroupID ?? "";
    message.minimumSignatures = object.minimumSignatures ?? 0;
    return message;
  },
};

function createBaseParallelApproversGroups(): ParallelApproversGroups {
  return { sequential: [] };
}

export const ParallelApproversGroups = {
  encode(message: ParallelApproversGroups, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.sequential) {
      ApproversGroup.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ParallelApproversGroups {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParallelApproversGroups();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sequential.push(ApproversGroup.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParallelApproversGroups {
    return {
      sequential: globalThis.Array.isArray(object?.sequential)
        ? object.sequential.map((e: any) => ApproversGroup.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ParallelApproversGroups): unknown {
    const obj: any = {};
    if (message.sequential?.length) {
      obj.sequential = message.sequential.map((e) => ApproversGroup.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ParallelApproversGroups>): ParallelApproversGroups {
    return ParallelApproversGroups.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParallelApproversGroups>): ParallelApproversGroups {
    const message = createBaseParallelApproversGroups();
    message.sequential = object.sequential?.map((e) => ApproversGroup.fromPartial(e)) || [];
    return message;
  },
};

function createBaseApprovers(): Approvers {
  return { parallel: [] };
}

export const Approvers = {
  encode(message: Approvers, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.parallel) {
      ParallelApproversGroups.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Approvers {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parallel.push(ParallelApproversGroups.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Approvers {
    return {
      parallel: globalThis.Array.isArray(object?.parallel)
        ? object.parallel.map((e: any) => ParallelApproversGroups.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Approvers): unknown {
    const obj: any = {};
    if (message.parallel?.length) {
      obj.parallel = message.parallel.map((e) => ParallelApproversGroups.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Approvers>): Approvers {
    return Approvers.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Approvers>): Approvers {
    const message = createBaseApprovers();
    message.parallel = object.parallel?.map((e) => ParallelApproversGroups.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
