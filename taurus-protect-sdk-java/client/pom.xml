<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.taurushq.sdk.protect</groupId>
        <artifactId>sdk-java-pom</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>client</artifactId>


    <dependencies>
        <dependency>
            <groupId>com.taurushq.sdk.protect</groupId>
            <artifactId>openapi</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.taurushq.sdk.protect</groupId>
            <artifactId>proto</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java-util</artifactId>
            <version>${protobuf-version}</version>
        </dependency>
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
            <version>${commons-codec-version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>${guava-version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson-version}</version>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct-version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit-version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- Exclude integration tests from default test run -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <excludes>
                        <exclude>**/*IntegrationTest.java</exclude>
                        <exclude>**/*E2ETest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!--
                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-javadoc-plugin</artifactId>
                            <version>3.10.1</version>
                            <executions>
                                <execution>
                                    <id>javadoc-jar</id>
                                    <phase>package</phase>
                                    <goals>
                                        <goal>jar</goal>
                                    </goals>
                                    <configuration>
                                        <source>1.8</source>
                                    </configuration>
                                </execution>
                            </executions>
                        </plugin>
                        -->


        </plugins>

    </build>

    <profiles>
        <!-- source/target 8 still compiles against the CURRENT JDK's class library, so on a
             JDK 9+ machine a Java 9+ API (List.of, String.repeat) resolves fine and the break
             only surfaces on a real JDK 8. The release flag pins the class library to 8 too,
             turning that into a local compile error. It is unknown to the JDK 8 javac, hence
             the JDK-conditional profile.

             Scoped to this module on purpose: it is the hand-written one. Applying it to the
             generated openapi/proto modules makes MapStruct fail to resolve the reactor's
             proto classes ("No implementation was created ... erroneous element
             Whitelist.WhitelistedAddressOrBuilder"), which does not happen against their
             installed jars. Verify with: mvn help:active-profiles -pl client -->
        <profile>
            <id>jdk9-plus-release</id>
            <activation>
                <jdk>[9,)</jdk>
            </activation>
            <properties>
                <maven.compiler.release>${java.version}</maven.compiler.release>
            </properties>
        </profile>

        <!-- JDK 9+ enforces strong encapsulation; Gson needs java.lang opened
             to deserialize ApiException (extends Exception) in ApiExceptionMapperTest.
             The flag is rejected by the JDK 8 launcher, so it is only added on JDK 9+. -->
        <profile>
            <id>jdk9-plus-add-opens</id>
            <activation>
                <jdk>[9,)</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>--add-opens java.base/java.lang=ALL-UNNAMED</argLine>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>