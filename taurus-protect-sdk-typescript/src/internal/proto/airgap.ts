// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: airgap.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";
import { RequestEnvelope, SignedRepliesEnvelope, UserSignature } from "./request_reply";

export const protobufPackage = "tgvalidatord";

/**
 * OutgoingAirGapFile is the protobuf payload returned by calling the outgoing air-gap endpoint. This will be read by the
 * signer CLI to request a signature by the cold HSM.
 */
export interface OutgoingAirGapFile {
  encryptedPayload: Uint8Array;
  wrappingPublicKey: string;
  signers: OutgoingAirGapFile_Signer[];
}

export interface OutgoingAirGapFile_Payload {
  metadata: AirGapMetadata | undefined;
  envelope: AirGapSignedEnvelope | undefined;
  signature: UserSignature | undefined;
}

export interface OutgoingAirGapFile_Signer {
  externalId: string;
  encryptedSharedKey: Uint8Array;
}

/**
 * IncomingAirGapFile is the protobuf payload created by the signer CLI after getting the different signatures for the
 * requests from the cold HSM. This is decrypted by the GUI or the CLI to submit the signatures via the incoming
 * endpoint.
 */
export interface IncomingAirGapFile {
  encryptedPayload: Uint8Array;
  wrappingPublicKey: string;
  importers: IncomingAirGapFile_Importer[];
}

export interface IncomingAirGapFile_Importer {
  externalId: string;
  encryptedSharedKey: Uint8Array;
}

export interface IncomingAirGapFile_Payload {
  metadata: AirGapMetadata | undefined;
  signedRequests: SignedRepliesEnvelope | undefined;
  rejectedRequests: RequestEnvelope[];
  signerTrail: AirGapTrail | undefined;
}

export interface IncomingAirGapFile_SignedPayload {
  payload: Uint8Array;
  signature: UserSignature | undefined;
}

export interface AirGapMetadata {
  value: Uint8Array;
  signature: Uint8Array;
}

export interface AirGapMetadata_Value {
  addresses: AirGapMetadata_Address[];
  exporterTrail: AirGapTrail | undefined;
}

export interface AirGapMetadata_Address {
  id: number;
  requestId: string;
}

export interface AirGapSignedEnvelope {
  id: string;
  payloadSignature: UserSignature | undefined;
  payload: Uint8Array;
}

export interface AirGapSignedEnvelope_Requests {
  ts: number;
  slot: number;
  rulesSignatures: UserSignature[];
  rules: Uint8Array;
  requests: AirGapSignedEnvelope_Request[];
}

export interface AirGapSignedEnvelope_Request {
  envelope: RequestEnvelope | undefined;
  cancellations: RequestEnvelope[];
}

export interface AirGapTrail {
  externalId: string;
  createdAt: Date | undefined;
}

function createBaseOutgoingAirGapFile(): OutgoingAirGapFile {
  return { encryptedPayload: new Uint8Array(0), wrappingPublicKey: "", signers: [] };
}

export const OutgoingAirGapFile = {
  encode(message: OutgoingAirGapFile, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.encryptedPayload.length !== 0) {
      writer.uint32(10).bytes(message.encryptedPayload);
    }
    if (message.wrappingPublicKey !== "") {
      writer.uint32(18).string(message.wrappingPublicKey);
    }
    for (const v of message.signers) {
      OutgoingAirGapFile_Signer.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OutgoingAirGapFile {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutgoingAirGapFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.encryptedPayload = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.wrappingPublicKey = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.signers.push(OutgoingAirGapFile_Signer.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OutgoingAirGapFile {
    return {
      encryptedPayload: isSet(object.encryptedPayload) ? bytesFromBase64(object.encryptedPayload) : new Uint8Array(0),
      wrappingPublicKey: isSet(object.wrappingPublicKey) ? globalThis.String(object.wrappingPublicKey) : "",
      signers: globalThis.Array.isArray(object?.signers)
        ? object.signers.map((e: any) => OutgoingAirGapFile_Signer.fromJSON(e))
        : [],
    };
  },

  toJSON(message: OutgoingAirGapFile): unknown {
    const obj: any = {};
    if (message.encryptedPayload.length !== 0) {
      obj.encryptedPayload = base64FromBytes(message.encryptedPayload);
    }
    if (message.wrappingPublicKey !== "") {
      obj.wrappingPublicKey = message.wrappingPublicKey;
    }
    if (message.signers?.length) {
      obj.signers = message.signers.map((e) => OutgoingAirGapFile_Signer.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<OutgoingAirGapFile>): OutgoingAirGapFile {
    return OutgoingAirGapFile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OutgoingAirGapFile>): OutgoingAirGapFile {
    const message = createBaseOutgoingAirGapFile();
    message.encryptedPayload = object.encryptedPayload ?? new Uint8Array(0);
    message.wrappingPublicKey = object.wrappingPublicKey ?? "";
    message.signers = object.signers?.map((e) => OutgoingAirGapFile_Signer.fromPartial(e)) || [];
    return message;
  },
};

function createBaseOutgoingAirGapFile_Payload(): OutgoingAirGapFile_Payload {
  return { metadata: undefined, envelope: undefined, signature: undefined };
}

export const OutgoingAirGapFile_Payload = {
  encode(message: OutgoingAirGapFile_Payload, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.metadata !== undefined) {
      AirGapMetadata.encode(message.metadata, writer.uint32(10).fork()).ldelim();
    }
    if (message.envelope !== undefined) {
      AirGapSignedEnvelope.encode(message.envelope, writer.uint32(18).fork()).ldelim();
    }
    if (message.signature !== undefined) {
      UserSignature.encode(message.signature, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OutgoingAirGapFile_Payload {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutgoingAirGapFile_Payload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metadata = AirGapMetadata.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.envelope = AirGapSignedEnvelope.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.signature = UserSignature.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OutgoingAirGapFile_Payload {
    return {
      metadata: isSet(object.metadata) ? AirGapMetadata.fromJSON(object.metadata) : undefined,
      envelope: isSet(object.envelope) ? AirGapSignedEnvelope.fromJSON(object.envelope) : undefined,
      signature: isSet(object.signature) ? UserSignature.fromJSON(object.signature) : undefined,
    };
  },

  toJSON(message: OutgoingAirGapFile_Payload): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = AirGapMetadata.toJSON(message.metadata);
    }
    if (message.envelope !== undefined) {
      obj.envelope = AirGapSignedEnvelope.toJSON(message.envelope);
    }
    if (message.signature !== undefined) {
      obj.signature = UserSignature.toJSON(message.signature);
    }
    return obj;
  },

  create(base?: DeepPartial<OutgoingAirGapFile_Payload>): OutgoingAirGapFile_Payload {
    return OutgoingAirGapFile_Payload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OutgoingAirGapFile_Payload>): OutgoingAirGapFile_Payload {
    const message = createBaseOutgoingAirGapFile_Payload();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? AirGapMetadata.fromPartial(object.metadata)
      : undefined;
    message.envelope = (object.envelope !== undefined && object.envelope !== null)
      ? AirGapSignedEnvelope.fromPartial(object.envelope)
      : undefined;
    message.signature = (object.signature !== undefined && object.signature !== null)
      ? UserSignature.fromPartial(object.signature)
      : undefined;
    return message;
  },
};

function createBaseOutgoingAirGapFile_Signer(): OutgoingAirGapFile_Signer {
  return { externalId: "", encryptedSharedKey: new Uint8Array(0) };
}

export const OutgoingAirGapFile_Signer = {
  encode(message: OutgoingAirGapFile_Signer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.externalId !== "") {
      writer.uint32(10).string(message.externalId);
    }
    if (message.encryptedSharedKey.length !== 0) {
      writer.uint32(18).bytes(message.encryptedSharedKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): OutgoingAirGapFile_Signer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOutgoingAirGapFile_Signer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.externalId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.encryptedSharedKey = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OutgoingAirGapFile_Signer {
    return {
      externalId: isSet(object.externalId) ? globalThis.String(object.externalId) : "",
      encryptedSharedKey: isSet(object.encryptedSharedKey)
        ? bytesFromBase64(object.encryptedSharedKey)
        : new Uint8Array(0),
    };
  },

  toJSON(message: OutgoingAirGapFile_Signer): unknown {
    const obj: any = {};
    if (message.externalId !== "") {
      obj.externalId = message.externalId;
    }
    if (message.encryptedSharedKey.length !== 0) {
      obj.encryptedSharedKey = base64FromBytes(message.encryptedSharedKey);
    }
    return obj;
  },

  create(base?: DeepPartial<OutgoingAirGapFile_Signer>): OutgoingAirGapFile_Signer {
    return OutgoingAirGapFile_Signer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OutgoingAirGapFile_Signer>): OutgoingAirGapFile_Signer {
    const message = createBaseOutgoingAirGapFile_Signer();
    message.externalId = object.externalId ?? "";
    message.encryptedSharedKey = object.encryptedSharedKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseIncomingAirGapFile(): IncomingAirGapFile {
  return { encryptedPayload: new Uint8Array(0), wrappingPublicKey: "", importers: [] };
}

export const IncomingAirGapFile = {
  encode(message: IncomingAirGapFile, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.encryptedPayload.length !== 0) {
      writer.uint32(10).bytes(message.encryptedPayload);
    }
    if (message.wrappingPublicKey !== "") {
      writer.uint32(18).string(message.wrappingPublicKey);
    }
    for (const v of message.importers) {
      IncomingAirGapFile_Importer.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncomingAirGapFile {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncomingAirGapFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.encryptedPayload = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.wrappingPublicKey = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.importers.push(IncomingAirGapFile_Importer.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncomingAirGapFile {
    return {
      encryptedPayload: isSet(object.encryptedPayload) ? bytesFromBase64(object.encryptedPayload) : new Uint8Array(0),
      wrappingPublicKey: isSet(object.wrappingPublicKey) ? globalThis.String(object.wrappingPublicKey) : "",
      importers: globalThis.Array.isArray(object?.importers)
        ? object.importers.map((e: any) => IncomingAirGapFile_Importer.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IncomingAirGapFile): unknown {
    const obj: any = {};
    if (message.encryptedPayload.length !== 0) {
      obj.encryptedPayload = base64FromBytes(message.encryptedPayload);
    }
    if (message.wrappingPublicKey !== "") {
      obj.wrappingPublicKey = message.wrappingPublicKey;
    }
    if (message.importers?.length) {
      obj.importers = message.importers.map((e) => IncomingAirGapFile_Importer.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<IncomingAirGapFile>): IncomingAirGapFile {
    return IncomingAirGapFile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IncomingAirGapFile>): IncomingAirGapFile {
    const message = createBaseIncomingAirGapFile();
    message.encryptedPayload = object.encryptedPayload ?? new Uint8Array(0);
    message.wrappingPublicKey = object.wrappingPublicKey ?? "";
    message.importers = object.importers?.map((e) => IncomingAirGapFile_Importer.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIncomingAirGapFile_Importer(): IncomingAirGapFile_Importer {
  return { externalId: "", encryptedSharedKey: new Uint8Array(0) };
}

export const IncomingAirGapFile_Importer = {
  encode(message: IncomingAirGapFile_Importer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.externalId !== "") {
      writer.uint32(10).string(message.externalId);
    }
    if (message.encryptedSharedKey.length !== 0) {
      writer.uint32(18).bytes(message.encryptedSharedKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncomingAirGapFile_Importer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncomingAirGapFile_Importer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.externalId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.encryptedSharedKey = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncomingAirGapFile_Importer {
    return {
      externalId: isSet(object.externalId) ? globalThis.String(object.externalId) : "",
      encryptedSharedKey: isSet(object.encryptedSharedKey)
        ? bytesFromBase64(object.encryptedSharedKey)
        : new Uint8Array(0),
    };
  },

  toJSON(message: IncomingAirGapFile_Importer): unknown {
    const obj: any = {};
    if (message.externalId !== "") {
      obj.externalId = message.externalId;
    }
    if (message.encryptedSharedKey.length !== 0) {
      obj.encryptedSharedKey = base64FromBytes(message.encryptedSharedKey);
    }
    return obj;
  },

  create(base?: DeepPartial<IncomingAirGapFile_Importer>): IncomingAirGapFile_Importer {
    return IncomingAirGapFile_Importer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IncomingAirGapFile_Importer>): IncomingAirGapFile_Importer {
    const message = createBaseIncomingAirGapFile_Importer();
    message.externalId = object.externalId ?? "";
    message.encryptedSharedKey = object.encryptedSharedKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseIncomingAirGapFile_Payload(): IncomingAirGapFile_Payload {
  return { metadata: undefined, signedRequests: undefined, rejectedRequests: [], signerTrail: undefined };
}

export const IncomingAirGapFile_Payload = {
  encode(message: IncomingAirGapFile_Payload, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.metadata !== undefined) {
      AirGapMetadata.encode(message.metadata, writer.uint32(10).fork()).ldelim();
    }
    if (message.signedRequests !== undefined) {
      SignedRepliesEnvelope.encode(message.signedRequests, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.rejectedRequests) {
      RequestEnvelope.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.signerTrail !== undefined) {
      AirGapTrail.encode(message.signerTrail, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncomingAirGapFile_Payload {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncomingAirGapFile_Payload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.metadata = AirGapMetadata.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signedRequests = SignedRepliesEnvelope.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.rejectedRequests.push(RequestEnvelope.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.signerTrail = AirGapTrail.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncomingAirGapFile_Payload {
    return {
      metadata: isSet(object.metadata) ? AirGapMetadata.fromJSON(object.metadata) : undefined,
      signedRequests: isSet(object.signedRequests) ? SignedRepliesEnvelope.fromJSON(object.signedRequests) : undefined,
      rejectedRequests: globalThis.Array.isArray(object?.rejectedRequests)
        ? object.rejectedRequests.map((e: any) => RequestEnvelope.fromJSON(e))
        : [],
      signerTrail: isSet(object.signerTrail) ? AirGapTrail.fromJSON(object.signerTrail) : undefined,
    };
  },

  toJSON(message: IncomingAirGapFile_Payload): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = AirGapMetadata.toJSON(message.metadata);
    }
    if (message.signedRequests !== undefined) {
      obj.signedRequests = SignedRepliesEnvelope.toJSON(message.signedRequests);
    }
    if (message.rejectedRequests?.length) {
      obj.rejectedRequests = message.rejectedRequests.map((e) => RequestEnvelope.toJSON(e));
    }
    if (message.signerTrail !== undefined) {
      obj.signerTrail = AirGapTrail.toJSON(message.signerTrail);
    }
    return obj;
  },

  create(base?: DeepPartial<IncomingAirGapFile_Payload>): IncomingAirGapFile_Payload {
    return IncomingAirGapFile_Payload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IncomingAirGapFile_Payload>): IncomingAirGapFile_Payload {
    const message = createBaseIncomingAirGapFile_Payload();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? AirGapMetadata.fromPartial(object.metadata)
      : undefined;
    message.signedRequests = (object.signedRequests !== undefined && object.signedRequests !== null)
      ? SignedRepliesEnvelope.fromPartial(object.signedRequests)
      : undefined;
    message.rejectedRequests = object.rejectedRequests?.map((e) => RequestEnvelope.fromPartial(e)) || [];
    message.signerTrail = (object.signerTrail !== undefined && object.signerTrail !== null)
      ? AirGapTrail.fromPartial(object.signerTrail)
      : undefined;
    return message;
  },
};

function createBaseIncomingAirGapFile_SignedPayload(): IncomingAirGapFile_SignedPayload {
  return { payload: new Uint8Array(0), signature: undefined };
}

export const IncomingAirGapFile_SignedPayload = {
  encode(message: IncomingAirGapFile_SignedPayload, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.signature !== undefined) {
      UserSignature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): IncomingAirGapFile_SignedPayload {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIncomingAirGapFile_SignedPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signature = UserSignature.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IncomingAirGapFile_SignedPayload {
    return {
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      signature: isSet(object.signature) ? UserSignature.fromJSON(object.signature) : undefined,
    };
  },

  toJSON(message: IncomingAirGapFile_SignedPayload): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.signature !== undefined) {
      obj.signature = UserSignature.toJSON(message.signature);
    }
    return obj;
  },

  create(base?: DeepPartial<IncomingAirGapFile_SignedPayload>): IncomingAirGapFile_SignedPayload {
    return IncomingAirGapFile_SignedPayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IncomingAirGapFile_SignedPayload>): IncomingAirGapFile_SignedPayload {
    const message = createBaseIncomingAirGapFile_SignedPayload();
    message.payload = object.payload ?? new Uint8Array(0);
    message.signature = (object.signature !== undefined && object.signature !== null)
      ? UserSignature.fromPartial(object.signature)
      : undefined;
    return message;
  },
};

function createBaseAirGapMetadata(): AirGapMetadata {
  return { value: new Uint8Array(0), signature: new Uint8Array(0) };
}

export const AirGapMetadata = {
  encode(message: AirGapMetadata, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value.length !== 0) {
      writer.uint32(10).bytes(message.value);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapMetadata {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = reader.bytes();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signature = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapMetadata {
    return {
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: AirGapMetadata): unknown {
    const obj: any = {};
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapMetadata>): AirGapMetadata {
    return AirGapMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapMetadata>): AirGapMetadata {
    const message = createBaseAirGapMetadata();
    message.value = object.value ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseAirGapMetadata_Value(): AirGapMetadata_Value {
  return { addresses: [], exporterTrail: undefined };
}

export const AirGapMetadata_Value = {
  encode(message: AirGapMetadata_Value, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.addresses) {
      AirGapMetadata_Address.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.exporterTrail !== undefined) {
      AirGapTrail.encode(message.exporterTrail, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapMetadata_Value {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapMetadata_Value();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.addresses.push(AirGapMetadata_Address.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.exporterTrail = AirGapTrail.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapMetadata_Value {
    return {
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => AirGapMetadata_Address.fromJSON(e))
        : [],
      exporterTrail: isSet(object.exporterTrail) ? AirGapTrail.fromJSON(object.exporterTrail) : undefined,
    };
  },

  toJSON(message: AirGapMetadata_Value): unknown {
    const obj: any = {};
    if (message.addresses?.length) {
      obj.addresses = message.addresses.map((e) => AirGapMetadata_Address.toJSON(e));
    }
    if (message.exporterTrail !== undefined) {
      obj.exporterTrail = AirGapTrail.toJSON(message.exporterTrail);
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapMetadata_Value>): AirGapMetadata_Value {
    return AirGapMetadata_Value.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapMetadata_Value>): AirGapMetadata_Value {
    const message = createBaseAirGapMetadata_Value();
    message.addresses = object.addresses?.map((e) => AirGapMetadata_Address.fromPartial(e)) || [];
    message.exporterTrail = (object.exporterTrail !== undefined && object.exporterTrail !== null)
      ? AirGapTrail.fromPartial(object.exporterTrail)
      : undefined;
    return message;
  },
};

function createBaseAirGapMetadata_Address(): AirGapMetadata_Address {
  return { id: 0, requestId: "" };
}

export const AirGapMetadata_Address = {
  encode(message: AirGapMetadata_Address, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapMetadata_Address {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapMetadata_Address();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapMetadata_Address {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      requestId: isSet(object.requestId) ? globalThis.String(object.requestId) : "",
    };
  },

  toJSON(message: AirGapMetadata_Address): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapMetadata_Address>): AirGapMetadata_Address {
    return AirGapMetadata_Address.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapMetadata_Address>): AirGapMetadata_Address {
    const message = createBaseAirGapMetadata_Address();
    message.id = object.id ?? 0;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseAirGapSignedEnvelope(): AirGapSignedEnvelope {
  return { id: "", payloadSignature: undefined, payload: new Uint8Array(0) };
}

export const AirGapSignedEnvelope = {
  encode(message: AirGapSignedEnvelope, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.payloadSignature !== undefined) {
      UserSignature.encode(message.payloadSignature, writer.uint32(18).fork()).ldelim();
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapSignedEnvelope {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapSignedEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.payloadSignature = UserSignature.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.payload = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapSignedEnvelope {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      payloadSignature: isSet(object.payloadSignature) ? UserSignature.fromJSON(object.payloadSignature) : undefined,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: AirGapSignedEnvelope): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.payloadSignature !== undefined) {
      obj.payloadSignature = UserSignature.toJSON(message.payloadSignature);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapSignedEnvelope>): AirGapSignedEnvelope {
    return AirGapSignedEnvelope.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapSignedEnvelope>): AirGapSignedEnvelope {
    const message = createBaseAirGapSignedEnvelope();
    message.id = object.id ?? "";
    message.payloadSignature = (object.payloadSignature !== undefined && object.payloadSignature !== null)
      ? UserSignature.fromPartial(object.payloadSignature)
      : undefined;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseAirGapSignedEnvelope_Requests(): AirGapSignedEnvelope_Requests {
  return { ts: 0, slot: 0, rulesSignatures: [], rules: new Uint8Array(0), requests: [] };
}

export const AirGapSignedEnvelope_Requests = {
  encode(message: AirGapSignedEnvelope_Requests, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ts !== 0) {
      writer.uint32(8).uint64(message.ts);
    }
    if (message.slot !== 0) {
      writer.uint32(16).uint32(message.slot);
    }
    for (const v of message.rulesSignatures) {
      UserSignature.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.rules.length !== 0) {
      writer.uint32(34).bytes(message.rules);
    }
    for (const v of message.requests) {
      AirGapSignedEnvelope_Request.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapSignedEnvelope_Requests {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapSignedEnvelope_Requests();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ts = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.rulesSignatures.push(UserSignature.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.rules = reader.bytes();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.requests.push(AirGapSignedEnvelope_Request.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapSignedEnvelope_Requests {
    return {
      ts: isSet(object.ts) ? globalThis.Number(object.ts) : 0,
      slot: isSet(object.slot) ? globalThis.Number(object.slot) : 0,
      rulesSignatures: globalThis.Array.isArray(object?.rulesSignatures)
        ? object.rulesSignatures.map((e: any) => UserSignature.fromJSON(e))
        : [],
      rules: isSet(object.rules) ? bytesFromBase64(object.rules) : new Uint8Array(0),
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => AirGapSignedEnvelope_Request.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AirGapSignedEnvelope_Requests): unknown {
    const obj: any = {};
    if (message.ts !== 0) {
      obj.ts = Math.round(message.ts);
    }
    if (message.slot !== 0) {
      obj.slot = Math.round(message.slot);
    }
    if (message.rulesSignatures?.length) {
      obj.rulesSignatures = message.rulesSignatures.map((e) => UserSignature.toJSON(e));
    }
    if (message.rules.length !== 0) {
      obj.rules = base64FromBytes(message.rules);
    }
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => AirGapSignedEnvelope_Request.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapSignedEnvelope_Requests>): AirGapSignedEnvelope_Requests {
    return AirGapSignedEnvelope_Requests.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapSignedEnvelope_Requests>): AirGapSignedEnvelope_Requests {
    const message = createBaseAirGapSignedEnvelope_Requests();
    message.ts = object.ts ?? 0;
    message.slot = object.slot ?? 0;
    message.rulesSignatures = object.rulesSignatures?.map((e) => UserSignature.fromPartial(e)) || [];
    message.rules = object.rules ?? new Uint8Array(0);
    message.requests = object.requests?.map((e) => AirGapSignedEnvelope_Request.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAirGapSignedEnvelope_Request(): AirGapSignedEnvelope_Request {
  return { envelope: undefined, cancellations: [] };
}

export const AirGapSignedEnvelope_Request = {
  encode(message: AirGapSignedEnvelope_Request, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.envelope !== undefined) {
      RequestEnvelope.encode(message.envelope, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.cancellations) {
      RequestEnvelope.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapSignedEnvelope_Request {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapSignedEnvelope_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.envelope = RequestEnvelope.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cancellations.push(RequestEnvelope.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapSignedEnvelope_Request {
    return {
      envelope: isSet(object.envelope) ? RequestEnvelope.fromJSON(object.envelope) : undefined,
      cancellations: globalThis.Array.isArray(object?.cancellations)
        ? object.cancellations.map((e: any) => RequestEnvelope.fromJSON(e))
        : [],
    };
  },

  toJSON(message: AirGapSignedEnvelope_Request): unknown {
    const obj: any = {};
    if (message.envelope !== undefined) {
      obj.envelope = RequestEnvelope.toJSON(message.envelope);
    }
    if (message.cancellations?.length) {
      obj.cancellations = message.cancellations.map((e) => RequestEnvelope.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapSignedEnvelope_Request>): AirGapSignedEnvelope_Request {
    return AirGapSignedEnvelope_Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapSignedEnvelope_Request>): AirGapSignedEnvelope_Request {
    const message = createBaseAirGapSignedEnvelope_Request();
    message.envelope = (object.envelope !== undefined && object.envelope !== null)
      ? RequestEnvelope.fromPartial(object.envelope)
      : undefined;
    message.cancellations = object.cancellations?.map((e) => RequestEnvelope.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAirGapTrail(): AirGapTrail {
  return { externalId: "", createdAt: undefined };
}

export const AirGapTrail = {
  encode(message: AirGapTrail, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.externalId !== "") {
      writer.uint32(10).string(message.externalId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AirGapTrail {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAirGapTrail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.externalId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AirGapTrail {
    return {
      externalId: isSet(object.externalId) ? globalThis.String(object.externalId) : "",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: AirGapTrail): unknown {
    const obj: any = {};
    if (message.externalId !== "") {
      obj.externalId = message.externalId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<AirGapTrail>): AirGapTrail {
    return AirGapTrail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AirGapTrail>): AirGapTrail {
    const message = createBaseAirGapTrail();
    message.externalId = object.externalId ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
