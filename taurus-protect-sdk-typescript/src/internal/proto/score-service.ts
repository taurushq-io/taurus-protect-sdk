// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: score-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Score } from "./score";

export const protobufPackage = "tgvalidatord";

export interface RefreshScoreRequest {
  addressId: number;
  scoreProvider: string;
}

export interface RefreshScoreReply {
  scores: Score[];
}

function createBaseRefreshScoreRequest(): RefreshScoreRequest {
  return { addressId: 0, scoreProvider: "" };
}

export const RefreshScoreRequest = {
  encode(message: RefreshScoreRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.addressId !== 0) {
      writer.uint32(8).uint64(message.addressId);
    }
    if (message.scoreProvider !== "") {
      writer.uint32(18).string(message.scoreProvider);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RefreshScoreRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshScoreRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.addressId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.scoreProvider = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshScoreRequest {
    return {
      addressId: isSet(object.addressId) ? globalThis.Number(object.addressId) : 0,
      scoreProvider: isSet(object.scoreProvider) ? globalThis.String(object.scoreProvider) : "",
    };
  },

  toJSON(message: RefreshScoreRequest): unknown {
    const obj: any = {};
    if (message.addressId !== 0) {
      obj.addressId = Math.round(message.addressId);
    }
    if (message.scoreProvider !== "") {
      obj.scoreProvider = message.scoreProvider;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshScoreRequest>): RefreshScoreRequest {
    return RefreshScoreRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshScoreRequest>): RefreshScoreRequest {
    const message = createBaseRefreshScoreRequest();
    message.addressId = object.addressId ?? 0;
    message.scoreProvider = object.scoreProvider ?? "";
    return message;
  },
};

function createBaseRefreshScoreReply(): RefreshScoreReply {
  return { scores: [] };
}

export const RefreshScoreReply = {
  encode(message: RefreshScoreReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.scores) {
      Score.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RefreshScoreReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshScoreReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.scores.push(Score.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshScoreReply {
    return { scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => Score.fromJSON(e)) : [] };
  },

  toJSON(message: RefreshScoreReply): unknown {
    const obj: any = {};
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => Score.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshScoreReply>): RefreshScoreReply {
    return RefreshScoreReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshScoreReply>): RefreshScoreReply {
    const message = createBaseRefreshScoreReply();
    message.scores = object.scores?.map((e) => Score.fromPartial(e)) || [];
    return message;
  },
};

export interface ScoreService {
  RefreshAddressScore(request: RefreshScoreRequest): Promise<RefreshScoreReply>;
  RefreshWLAScore(request: RefreshScoreRequest): Promise<RefreshScoreReply>;
}

export const ScoreServiceServiceName = "tgvalidatord.ScoreService";
export class ScoreServiceClientImpl implements ScoreService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ScoreServiceServiceName;
    this.rpc = rpc;
    this.RefreshAddressScore = this.RefreshAddressScore.bind(this);
    this.RefreshWLAScore = this.RefreshWLAScore.bind(this);
  }
  RefreshAddressScore(request: RefreshScoreRequest): Promise<RefreshScoreReply> {
    const data = RefreshScoreRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RefreshAddressScore", data);
    return promise.then((data) => RefreshScoreReply.decode(_m0.Reader.create(data)));
  }

  RefreshWLAScore(request: RefreshScoreRequest): Promise<RefreshScoreReply> {
    const data = RefreshScoreRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RefreshWLAScore", data);
    return promise.then((data) => RefreshScoreReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
