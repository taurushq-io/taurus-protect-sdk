// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: sso.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface SecurityDomain {
  id: number;
  tenantId: number;
  domain: string;
  mode: SecurityDomain_Mode;
  attributes: SecurityDomainAttribute[];
  creationDate: Date | undefined;
  updateDate: Date | undefined;
  enabled: boolean;
}

export enum SecurityDomain_Mode {
  Undefined = 0,
  Basic = 1,
  OIDC = 2,
  SAML = 3,
  UNRECOGNIZED = -1,
}

export function securityDomain_ModeFromJSON(object: any): SecurityDomain_Mode {
  switch (object) {
    case 0:
    case "Undefined":
      return SecurityDomain_Mode.Undefined;
    case 1:
    case "Basic":
      return SecurityDomain_Mode.Basic;
    case 2:
    case "OIDC":
      return SecurityDomain_Mode.OIDC;
    case 3:
    case "SAML":
      return SecurityDomain_Mode.SAML;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SecurityDomain_Mode.UNRECOGNIZED;
  }
}

export function securityDomain_ModeToJSON(object: SecurityDomain_Mode): string {
  switch (object) {
    case SecurityDomain_Mode.Undefined:
      return "Undefined";
    case SecurityDomain_Mode.Basic:
      return "Basic";
    case SecurityDomain_Mode.OIDC:
      return "OIDC";
    case SecurityDomain_Mode.SAML:
      return "SAML";
    case SecurityDomain_Mode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface SecurityDomainAttribute {
  id: number;
  key: string;
  value: string;
  creationDate: Date | undefined;
  updateDate: Date | undefined;
}

function createBaseSecurityDomain(): SecurityDomain {
  return {
    id: 0,
    tenantId: 0,
    domain: "",
    mode: 0,
    attributes: [],
    creationDate: undefined,
    updateDate: undefined,
    enabled: false,
  };
}

export const SecurityDomain = {
  encode(message: SecurityDomain, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.tenantId !== 0) {
      writer.uint32(16).uint64(message.tenantId);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    if (message.mode !== 0) {
      writer.uint32(32).int32(message.mode);
    }
    for (const v of message.attributes) {
      SecurityDomainAttribute.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(50).fork()).ldelim();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(58).fork()).ldelim();
    }
    if (message.enabled !== false) {
      writer.uint32(64).bool(message.enabled);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SecurityDomain {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecurityDomain();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.domain = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.attributes.push(SecurityDomainAttribute.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.enabled = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecurityDomain {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      mode: isSet(object.mode) ? securityDomain_ModeFromJSON(object.mode) : 0,
      attributes: globalThis.Array.isArray(object?.attributes)
        ? object.attributes.map((e: any) => SecurityDomainAttribute.fromJSON(e))
        : [],
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      updateDate: isSet(object.updateDate) ? fromJsonTimestamp(object.updateDate) : undefined,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: SecurityDomain): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.mode !== 0) {
      obj.mode = securityDomain_ModeToJSON(message.mode);
    }
    if (message.attributes?.length) {
      obj.attributes = message.attributes.map((e) => SecurityDomainAttribute.toJSON(e));
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.updateDate !== undefined) {
      obj.updateDate = message.updateDate.toISOString();
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<SecurityDomain>): SecurityDomain {
    return SecurityDomain.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SecurityDomain>): SecurityDomain {
    const message = createBaseSecurityDomain();
    message.id = object.id ?? 0;
    message.tenantId = object.tenantId ?? 0;
    message.domain = object.domain ?? "";
    message.mode = object.mode ?? 0;
    message.attributes = object.attributes?.map((e) => SecurityDomainAttribute.fromPartial(e)) || [];
    message.creationDate = object.creationDate ?? undefined;
    message.updateDate = object.updateDate ?? undefined;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseSecurityDomainAttribute(): SecurityDomainAttribute {
  return { id: 0, key: "", value: "", creationDate: undefined, updateDate: undefined };
}

export const SecurityDomainAttribute = {
  encode(message: SecurityDomainAttribute, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(26).string(message.value);
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(34).fork()).ldelim();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SecurityDomainAttribute {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecurityDomainAttribute();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.value = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecurityDomainAttribute {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      updateDate: isSet(object.updateDate) ? fromJsonTimestamp(object.updateDate) : undefined,
    };
  },

  toJSON(message: SecurityDomainAttribute): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.updateDate !== undefined) {
      obj.updateDate = message.updateDate.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<SecurityDomainAttribute>): SecurityDomainAttribute {
    return SecurityDomainAttribute.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SecurityDomainAttribute>): SecurityDomainAttribute {
    const message = createBaseSecurityDomainAttribute();
    message.id = object.id ?? 0;
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    message.creationDate = object.creationDate ?? undefined;
    message.updateDate = object.updateDate ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
