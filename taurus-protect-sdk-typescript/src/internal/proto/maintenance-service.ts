// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: maintenance-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Empty } from "./google/protobuf/empty";

export const protobufPackage = "tgvalidatord";

export interface RecomputeWalletsBalancesRequest {
  tenantId: number;
  walletIds: number[];
}

export interface TriggerExistentialRecomputeRequest {
  tenantId: number;
  currencyId: string;
}

export interface TriggerExistentialRecomputeReply {
  message: string;
}

export interface ForceKillRequestRequest {
  tenantId: number;
  requestId: number;
}

export interface ForceKillBundleRequest {
  tenantId: number;
  requestBundleId: number;
}

function createBaseRecomputeWalletsBalancesRequest(): RecomputeWalletsBalancesRequest {
  return { tenantId: 0, walletIds: [] };
}

export const RecomputeWalletsBalancesRequest = {
  encode(message: RecomputeWalletsBalancesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.tenantId !== 0) {
      writer.uint32(8).uint64(message.tenantId);
    }
    writer.uint32(18).fork();
    for (const v of message.walletIds) {
      writer.uint64(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RecomputeWalletsBalancesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecomputeWalletsBalancesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag === 16) {
            message.walletIds.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.walletIds.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecomputeWalletsBalancesRequest {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      walletIds: globalThis.Array.isArray(object?.walletIds)
        ? object.walletIds.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: RecomputeWalletsBalancesRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.walletIds?.length) {
      obj.walletIds = message.walletIds.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RecomputeWalletsBalancesRequest>): RecomputeWalletsBalancesRequest {
    return RecomputeWalletsBalancesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecomputeWalletsBalancesRequest>): RecomputeWalletsBalancesRequest {
    const message = createBaseRecomputeWalletsBalancesRequest();
    message.tenantId = object.tenantId ?? 0;
    message.walletIds = object.walletIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseTriggerExistentialRecomputeRequest(): TriggerExistentialRecomputeRequest {
  return { tenantId: 0, currencyId: "" };
}

export const TriggerExistentialRecomputeRequest = {
  encode(message: TriggerExistentialRecomputeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.tenantId !== 0) {
      writer.uint32(8).uint64(message.tenantId);
    }
    if (message.currencyId !== "") {
      writer.uint32(18).string(message.currencyId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TriggerExistentialRecomputeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerExistentialRecomputeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.currencyId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerExistentialRecomputeRequest {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      currencyId: isSet(object.currencyId) ? globalThis.String(object.currencyId) : "",
    };
  },

  toJSON(message: TriggerExistentialRecomputeRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.currencyId !== "") {
      obj.currencyId = message.currencyId;
    }
    return obj;
  },

  create(base?: DeepPartial<TriggerExistentialRecomputeRequest>): TriggerExistentialRecomputeRequest {
    return TriggerExistentialRecomputeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TriggerExistentialRecomputeRequest>): TriggerExistentialRecomputeRequest {
    const message = createBaseTriggerExistentialRecomputeRequest();
    message.tenantId = object.tenantId ?? 0;
    message.currencyId = object.currencyId ?? "";
    return message;
  },
};

function createBaseTriggerExistentialRecomputeReply(): TriggerExistentialRecomputeReply {
  return { message: "" };
}

export const TriggerExistentialRecomputeReply = {
  encode(message: TriggerExistentialRecomputeReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TriggerExistentialRecomputeReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerExistentialRecomputeReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerExistentialRecomputeReply {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: TriggerExistentialRecomputeReply): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<TriggerExistentialRecomputeReply>): TriggerExistentialRecomputeReply {
    return TriggerExistentialRecomputeReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TriggerExistentialRecomputeReply>): TriggerExistentialRecomputeReply {
    const message = createBaseTriggerExistentialRecomputeReply();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseForceKillRequestRequest(): ForceKillRequestRequest {
  return { tenantId: 0, requestId: 0 };
}

export const ForceKillRequestRequest = {
  encode(message: ForceKillRequestRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.tenantId !== 0) {
      writer.uint32(8).uint64(message.tenantId);
    }
    if (message.requestId !== 0) {
      writer.uint32(16).uint64(message.requestId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ForceKillRequestRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseForceKillRequestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.requestId = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ForceKillRequestRequest {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      requestId: isSet(object.requestId) ? globalThis.Number(object.requestId) : 0,
    };
  },

  toJSON(message: ForceKillRequestRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    return obj;
  },

  create(base?: DeepPartial<ForceKillRequestRequest>): ForceKillRequestRequest {
    return ForceKillRequestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ForceKillRequestRequest>): ForceKillRequestRequest {
    const message = createBaseForceKillRequestRequest();
    message.tenantId = object.tenantId ?? 0;
    message.requestId = object.requestId ?? 0;
    return message;
  },
};

function createBaseForceKillBundleRequest(): ForceKillBundleRequest {
  return { tenantId: 0, requestBundleId: 0 };
}

export const ForceKillBundleRequest = {
  encode(message: ForceKillBundleRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.tenantId !== 0) {
      writer.uint32(8).uint64(message.tenantId);
    }
    if (message.requestBundleId !== 0) {
      writer.uint32(16).uint64(message.requestBundleId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ForceKillBundleRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseForceKillBundleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.requestBundleId = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ForceKillBundleRequest {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      requestBundleId: isSet(object.requestBundleId) ? globalThis.Number(object.requestBundleId) : 0,
    };
  },

  toJSON(message: ForceKillBundleRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.requestBundleId !== 0) {
      obj.requestBundleId = Math.round(message.requestBundleId);
    }
    return obj;
  },

  create(base?: DeepPartial<ForceKillBundleRequest>): ForceKillBundleRequest {
    return ForceKillBundleRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ForceKillBundleRequest>): ForceKillBundleRequest {
    const message = createBaseForceKillBundleRequest();
    message.tenantId = object.tenantId ?? 0;
    message.requestBundleId = object.requestBundleId ?? 0;
    return message;
  },
};

export interface MaintenanceService {
  RecomputeWalletsBalances(request: RecomputeWalletsBalancesRequest): Promise<Empty>;
  TriggerExistentialRecompute(request: TriggerExistentialRecomputeRequest): Promise<TriggerExistentialRecomputeReply>;
  ForceKillRequest(request: ForceKillRequestRequest): Promise<Empty>;
  ForceKillBundle(request: ForceKillBundleRequest): Promise<Empty>;
}

export const MaintenanceServiceServiceName = "tgvalidatord.MaintenanceService";
export class MaintenanceServiceClientImpl implements MaintenanceService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || MaintenanceServiceServiceName;
    this.rpc = rpc;
    this.RecomputeWalletsBalances = this.RecomputeWalletsBalances.bind(this);
    this.TriggerExistentialRecompute = this.TriggerExistentialRecompute.bind(this);
    this.ForceKillRequest = this.ForceKillRequest.bind(this);
    this.ForceKillBundle = this.ForceKillBundle.bind(this);
  }
  RecomputeWalletsBalances(request: RecomputeWalletsBalancesRequest): Promise<Empty> {
    const data = RecomputeWalletsBalancesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RecomputeWalletsBalances", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  TriggerExistentialRecompute(request: TriggerExistentialRecomputeRequest): Promise<TriggerExistentialRecomputeReply> {
    const data = TriggerExistentialRecomputeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "TriggerExistentialRecompute", data);
    return promise.then((data) => TriggerExistentialRecomputeReply.decode(_m0.Reader.create(data)));
  }

  ForceKillRequest(request: ForceKillRequestRequest): Promise<Empty> {
    const data = ForceKillRequestRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ForceKillRequest", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  ForceKillBundle(request: ForceKillBundleRequest): Promise<Empty> {
    const data = ForceKillBundleRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ForceKillBundle", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
