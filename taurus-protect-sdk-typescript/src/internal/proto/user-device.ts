// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: user-device.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";

export const protobufPackage = "tgvalidatord";

export interface UserDevicePairingInfo {
  status: UserDevicePairingInfo_Status;
  pairingID: string;
  apiKey: string;
}

export enum UserDevicePairingInfo_Status {
  NONE = 0,
  WAITING = 1,
  PAIRING = 2,
  APPROVED = 3,
  UNRECOGNIZED = -1,
}

export function userDevicePairingInfo_StatusFromJSON(object: any): UserDevicePairingInfo_Status {
  switch (object) {
    case 0:
    case "NONE":
      return UserDevicePairingInfo_Status.NONE;
    case 1:
    case "WAITING":
      return UserDevicePairingInfo_Status.WAITING;
    case 2:
    case "PAIRING":
      return UserDevicePairingInfo_Status.PAIRING;
    case 3:
    case "APPROVED":
      return UserDevicePairingInfo_Status.APPROVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UserDevicePairingInfo_Status.UNRECOGNIZED;
  }
}

export function userDevicePairingInfo_StatusToJSON(object: UserDevicePairingInfo_Status): string {
  switch (object) {
    case UserDevicePairingInfo_Status.NONE:
      return "NONE";
    case UserDevicePairingInfo_Status.WAITING:
      return "WAITING";
    case UserDevicePairingInfo_Status.PAIRING:
      return "PAIRING";
    case UserDevicePairingInfo_Status.APPROVED:
      return "APPROVED";
    case UserDevicePairingInfo_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseUserDevicePairingInfo(): UserDevicePairingInfo {
  return { status: 0, pairingID: "", apiKey: "" };
}

export const UserDevicePairingInfo = {
  encode(message: UserDevicePairingInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.pairingID !== "") {
      writer.uint32(18).string(message.pairingID);
    }
    if (message.apiKey !== "") {
      writer.uint32(26).string(message.apiKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UserDevicePairingInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserDevicePairingInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pairingID = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.apiKey = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserDevicePairingInfo {
    return {
      status: isSet(object.status) ? userDevicePairingInfo_StatusFromJSON(object.status) : 0,
      pairingID: isSet(object.pairingID) ? globalThis.String(object.pairingID) : "",
      apiKey: isSet(object.apiKey) ? globalThis.String(object.apiKey) : "",
    };
  },

  toJSON(message: UserDevicePairingInfo): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = userDevicePairingInfo_StatusToJSON(message.status);
    }
    if (message.pairingID !== "") {
      obj.pairingID = message.pairingID;
    }
    if (message.apiKey !== "") {
      obj.apiKey = message.apiKey;
    }
    return obj;
  },

  create(base?: DeepPartial<UserDevicePairingInfo>): UserDevicePairingInfo {
    return UserDevicePairingInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserDevicePairingInfo>): UserDevicePairingInfo {
    const message = createBaseUserDevicePairingInfo();
    message.status = object.status ?? 0;
    message.pairingID = object.pairingID ?? "";
    message.apiKey = object.apiKey ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
