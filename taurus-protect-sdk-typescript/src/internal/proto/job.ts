// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: job.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Duration } from "./google/protobuf/duration";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface Job {
  name: string;
  statistics: JobStatistics | undefined;
}

export interface JobStatistics {
  pending: number;
  successes: number;
  failures: number;
  lastSuccess: JobStatus | undefined;
  lastFailure: JobStatus | undefined;
  avgDuration: Duration | undefined;
  maxDuration: Duration | undefined;
  minDuration: Duration | undefined;
}

export interface JobStatus {
  id: string;
  startedAt: Date | undefined;
  updatedAt: Date | undefined;
  timeoutAt: Date | undefined;
  message: string;
  status: string;
}

function createBaseJob(): Job {
  return { name: "", statistics: undefined };
}

export const Job = {
  encode(message: Job, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.statistics !== undefined) {
      JobStatistics.encode(message.statistics, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Job {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.statistics = JobStatistics.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Job {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      statistics: isSet(object.statistics) ? JobStatistics.fromJSON(object.statistics) : undefined,
    };
  },

  toJSON(message: Job): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.statistics !== undefined) {
      obj.statistics = JobStatistics.toJSON(message.statistics);
    }
    return obj;
  },

  create(base?: DeepPartial<Job>): Job {
    return Job.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Job>): Job {
    const message = createBaseJob();
    message.name = object.name ?? "";
    message.statistics = (object.statistics !== undefined && object.statistics !== null)
      ? JobStatistics.fromPartial(object.statistics)
      : undefined;
    return message;
  },
};

function createBaseJobStatistics(): JobStatistics {
  return {
    pending: 0,
    successes: 0,
    failures: 0,
    lastSuccess: undefined,
    lastFailure: undefined,
    avgDuration: undefined,
    maxDuration: undefined,
    minDuration: undefined,
  };
}

export const JobStatistics = {
  encode(message: JobStatistics, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pending !== 0) {
      writer.uint32(8).uint64(message.pending);
    }
    if (message.successes !== 0) {
      writer.uint32(16).uint64(message.successes);
    }
    if (message.failures !== 0) {
      writer.uint32(24).uint64(message.failures);
    }
    if (message.lastSuccess !== undefined) {
      JobStatus.encode(message.lastSuccess, writer.uint32(34).fork()).ldelim();
    }
    if (message.lastFailure !== undefined) {
      JobStatus.encode(message.lastFailure, writer.uint32(42).fork()).ldelim();
    }
    if (message.avgDuration !== undefined) {
      Duration.encode(message.avgDuration, writer.uint32(50).fork()).ldelim();
    }
    if (message.maxDuration !== undefined) {
      Duration.encode(message.maxDuration, writer.uint32(58).fork()).ldelim();
    }
    if (message.minDuration !== undefined) {
      Duration.encode(message.minDuration, writer.uint32(66).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JobStatistics {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStatistics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.pending = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.successes = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.failures = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.lastSuccess = JobStatus.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.lastFailure = JobStatus.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.avgDuration = Duration.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.maxDuration = Duration.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.minDuration = Duration.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobStatistics {
    return {
      pending: isSet(object.pending) ? globalThis.Number(object.pending) : 0,
      successes: isSet(object.successes) ? globalThis.Number(object.successes) : 0,
      failures: isSet(object.failures) ? globalThis.Number(object.failures) : 0,
      lastSuccess: isSet(object.lastSuccess) ? JobStatus.fromJSON(object.lastSuccess) : undefined,
      lastFailure: isSet(object.lastFailure) ? JobStatus.fromJSON(object.lastFailure) : undefined,
      avgDuration: isSet(object.avgDuration) ? Duration.fromJSON(object.avgDuration) : undefined,
      maxDuration: isSet(object.maxDuration) ? Duration.fromJSON(object.maxDuration) : undefined,
      minDuration: isSet(object.minDuration) ? Duration.fromJSON(object.minDuration) : undefined,
    };
  },

  toJSON(message: JobStatistics): unknown {
    const obj: any = {};
    if (message.pending !== 0) {
      obj.pending = Math.round(message.pending);
    }
    if (message.successes !== 0) {
      obj.successes = Math.round(message.successes);
    }
    if (message.failures !== 0) {
      obj.failures = Math.round(message.failures);
    }
    if (message.lastSuccess !== undefined) {
      obj.lastSuccess = JobStatus.toJSON(message.lastSuccess);
    }
    if (message.lastFailure !== undefined) {
      obj.lastFailure = JobStatus.toJSON(message.lastFailure);
    }
    if (message.avgDuration !== undefined) {
      obj.avgDuration = Duration.toJSON(message.avgDuration);
    }
    if (message.maxDuration !== undefined) {
      obj.maxDuration = Duration.toJSON(message.maxDuration);
    }
    if (message.minDuration !== undefined) {
      obj.minDuration = Duration.toJSON(message.minDuration);
    }
    return obj;
  },

  create(base?: DeepPartial<JobStatistics>): JobStatistics {
    return JobStatistics.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobStatistics>): JobStatistics {
    const message = createBaseJobStatistics();
    message.pending = object.pending ?? 0;
    message.successes = object.successes ?? 0;
    message.failures = object.failures ?? 0;
    message.lastSuccess = (object.lastSuccess !== undefined && object.lastSuccess !== null)
      ? JobStatus.fromPartial(object.lastSuccess)
      : undefined;
    message.lastFailure = (object.lastFailure !== undefined && object.lastFailure !== null)
      ? JobStatus.fromPartial(object.lastFailure)
      : undefined;
    message.avgDuration = (object.avgDuration !== undefined && object.avgDuration !== null)
      ? Duration.fromPartial(object.avgDuration)
      : undefined;
    message.maxDuration = (object.maxDuration !== undefined && object.maxDuration !== null)
      ? Duration.fromPartial(object.maxDuration)
      : undefined;
    message.minDuration = (object.minDuration !== undefined && object.minDuration !== null)
      ? Duration.fromPartial(object.minDuration)
      : undefined;
    return message;
  },
};

function createBaseJobStatus(): JobStatus {
  return { id: "", startedAt: undefined, updatedAt: undefined, timeoutAt: undefined, message: "", status: "" };
}

export const JobStatus = {
  encode(message: JobStatus, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(18).fork()).ldelim();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).ldelim();
    }
    if (message.timeoutAt !== undefined) {
      Timestamp.encode(toTimestamp(message.timeoutAt), writer.uint32(34).fork()).ldelim();
    }
    if (message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    if (message.status !== "") {
      writer.uint32(50).string(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JobStatus {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.timeoutAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.status = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobStatus {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      timeoutAt: isSet(object.timeoutAt) ? fromJsonTimestamp(object.timeoutAt) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: JobStatus): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.timeoutAt !== undefined) {
      obj.timeoutAt = message.timeoutAt.toISOString();
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<JobStatus>): JobStatus {
    return JobStatus.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobStatus>): JobStatus {
    const message = createBaseJobStatus();
    message.id = object.id ?? "";
    message.startedAt = object.startedAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.timeoutAt = object.timeoutAt ?? undefined;
    message.message = object.message ?? "";
    message.status = object.status ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
