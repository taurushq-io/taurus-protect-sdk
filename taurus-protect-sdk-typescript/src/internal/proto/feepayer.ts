// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: feepayer.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface FeePayerEnvelope {
  id: number;
  tenantId: number;
  blockchain: string;
  name: string;
  feePayer: FeePayer | undefined;
  creationDate: Date | undefined;
  network: string;
}

export interface FeePayer {
  blockchain: string;
  eth: FeePayer_Eth | undefined;
}

export enum FeePayer_Blockchain {
  NoneBlockchain = 0,
  ETH = 1,
  UNRECOGNIZED = -1,
}

export function feePayer_BlockchainFromJSON(object: any): FeePayer_Blockchain {
  switch (object) {
    case 0:
    case "NoneBlockchain":
      return FeePayer_Blockchain.NoneBlockchain;
    case 1:
    case "ETH":
      return FeePayer_Blockchain.ETH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FeePayer_Blockchain.UNRECOGNIZED;
  }
}

export function feePayer_BlockchainToJSON(object: FeePayer_Blockchain): string {
  switch (object) {
    case FeePayer_Blockchain.NoneBlockchain:
      return "NoneBlockchain";
    case FeePayer_Blockchain.ETH:
      return "ETH";
    case FeePayer_Blockchain.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum FeePayer_Kind {
  NoneKind = 0,
  Local = 1,
  Remote = 2,
  UNRECOGNIZED = -1,
}

export function feePayer_KindFromJSON(object: any): FeePayer_Kind {
  switch (object) {
    case 0:
    case "NoneKind":
      return FeePayer_Kind.NoneKind;
    case 1:
    case "Local":
      return FeePayer_Kind.Local;
    case 2:
    case "Remote":
      return FeePayer_Kind.Remote;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FeePayer_Kind.UNRECOGNIZED;
  }
}

export function feePayer_KindToJSON(object: FeePayer_Kind): string {
  switch (object) {
    case FeePayer_Kind.NoneKind:
      return "NoneKind";
    case FeePayer_Kind.Local:
      return "Local";
    case FeePayer_Kind.Remote:
      return "Remote";
    case FeePayer_Kind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum FeePayer_ForwarderKind {
  GSNForwarderV2 = 0,
  OpenZeppelinForwarder = 1,
  UNRECOGNIZED = -1,
}

export function feePayer_ForwarderKindFromJSON(object: any): FeePayer_ForwarderKind {
  switch (object) {
    case 0:
    case "GSNForwarderV2":
      return FeePayer_ForwarderKind.GSNForwarderV2;
    case 1:
    case "OpenZeppelinForwarder":
      return FeePayer_ForwarderKind.OpenZeppelinForwarder;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FeePayer_ForwarderKind.UNRECOGNIZED;
  }
}

export function feePayer_ForwarderKindToJSON(object: FeePayer_ForwarderKind): string {
  switch (object) {
    case FeePayer_ForwarderKind.GSNForwarderV2:
      return "GSNForwarderV2";
    case FeePayer_ForwarderKind.OpenZeppelinForwarder:
      return "OpenZeppelinForwarder";
    case FeePayer_ForwarderKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface FeePayer_Eth {
  kind: string;
  local:
    | FeePayer_Eth_Local
    | undefined;
  /** Used when unpacking */
  remote: FeePayer_Eth_Remote | undefined;
  remoteEncrypted: Uint8Array;
}

export interface FeePayer_Eth_Local {
  addressId: number;
  forwarderAddressId: number;
  autoApprove: boolean;
  creatorAddressId: number;
  forwarderKind: FeePayer_ForwarderKind;
  domainSeparator: Uint8Array;
}

export interface FeePayer_Eth_Remote {
  url: string;
  username: string;
  password: string;
  privateKey: string;
  fromAddressId: number;
  forwarderAddress: string;
  forwarderAddressId: number;
  creatorAddress: string;
  creatorAddressId: number;
  forwarderKind: FeePayer_ForwarderKind;
  domainSeparator: Uint8Array;
}

function createBaseFeePayerEnvelope(): FeePayerEnvelope {
  return { id: 0, tenantId: 0, blockchain: "", name: "", feePayer: undefined, creationDate: undefined, network: "" };
}

export const FeePayerEnvelope = {
  encode(message: FeePayerEnvelope, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.tenantId !== 0) {
      writer.uint32(16).uint64(message.tenantId);
    }
    if (message.blockchain !== "") {
      writer.uint32(26).string(message.blockchain);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    if (message.feePayer !== undefined) {
      FeePayer.encode(message.feePayer, writer.uint32(42).fork()).ldelim();
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(50).fork()).ldelim();
    }
    if (message.network !== "") {
      writer.uint32(58).string(message.network);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeePayerEnvelope {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeePayerEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.feePayer = FeePayer.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.network = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeePayerEnvelope {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      feePayer: isSet(object.feePayer) ? FeePayer.fromJSON(object.feePayer) : undefined,
      creationDate: isSet(object.creationDate) ? fromJsonTimestamp(object.creationDate) : undefined,
      network: isSet(object.network) ? globalThis.String(object.network) : "",
    };
  },

  toJSON(message: FeePayerEnvelope): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.feePayer !== undefined) {
      obj.feePayer = FeePayer.toJSON(message.feePayer);
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    return obj;
  },

  create(base?: DeepPartial<FeePayerEnvelope>): FeePayerEnvelope {
    return FeePayerEnvelope.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeePayerEnvelope>): FeePayerEnvelope {
    const message = createBaseFeePayerEnvelope();
    message.id = object.id ?? 0;
    message.tenantId = object.tenantId ?? 0;
    message.blockchain = object.blockchain ?? "";
    message.name = object.name ?? "";
    message.feePayer = (object.feePayer !== undefined && object.feePayer !== null)
      ? FeePayer.fromPartial(object.feePayer)
      : undefined;
    message.creationDate = object.creationDate ?? undefined;
    message.network = object.network ?? "";
    return message;
  },
};

function createBaseFeePayer(): FeePayer {
  return { blockchain: "", eth: undefined };
}

export const FeePayer = {
  encode(message: FeePayer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.blockchain !== "") {
      writer.uint32(10).string(message.blockchain);
    }
    if (message.eth !== undefined) {
      FeePayer_Eth.encode(message.eth, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeePayer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeePayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.eth = FeePayer_Eth.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeePayer {
    return {
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      eth: isSet(object.eth) ? FeePayer_Eth.fromJSON(object.eth) : undefined,
    };
  },

  toJSON(message: FeePayer): unknown {
    const obj: any = {};
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.eth !== undefined) {
      obj.eth = FeePayer_Eth.toJSON(message.eth);
    }
    return obj;
  },

  create(base?: DeepPartial<FeePayer>): FeePayer {
    return FeePayer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeePayer>): FeePayer {
    const message = createBaseFeePayer();
    message.blockchain = object.blockchain ?? "";
    message.eth = (object.eth !== undefined && object.eth !== null) ? FeePayer_Eth.fromPartial(object.eth) : undefined;
    return message;
  },
};

function createBaseFeePayer_Eth(): FeePayer_Eth {
  return { kind: "", local: undefined, remote: undefined, remoteEncrypted: new Uint8Array(0) };
}

export const FeePayer_Eth = {
  encode(message: FeePayer_Eth, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.local !== undefined) {
      FeePayer_Eth_Local.encode(message.local, writer.uint32(18).fork()).ldelim();
    }
    if (message.remote !== undefined) {
      FeePayer_Eth_Remote.encode(message.remote, writer.uint32(26).fork()).ldelim();
    }
    if (message.remoteEncrypted.length !== 0) {
      writer.uint32(34).bytes(message.remoteEncrypted);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeePayer_Eth {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeePayer_Eth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.local = FeePayer_Eth_Local.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.remote = FeePayer_Eth_Remote.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.remoteEncrypted = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeePayer_Eth {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      local: isSet(object.local) ? FeePayer_Eth_Local.fromJSON(object.local) : undefined,
      remote: isSet(object.remote) ? FeePayer_Eth_Remote.fromJSON(object.remote) : undefined,
      remoteEncrypted: isSet(object.remoteEncrypted) ? bytesFromBase64(object.remoteEncrypted) : new Uint8Array(0),
    };
  },

  toJSON(message: FeePayer_Eth): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.local !== undefined) {
      obj.local = FeePayer_Eth_Local.toJSON(message.local);
    }
    if (message.remote !== undefined) {
      obj.remote = FeePayer_Eth_Remote.toJSON(message.remote);
    }
    if (message.remoteEncrypted.length !== 0) {
      obj.remoteEncrypted = base64FromBytes(message.remoteEncrypted);
    }
    return obj;
  },

  create(base?: DeepPartial<FeePayer_Eth>): FeePayer_Eth {
    return FeePayer_Eth.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeePayer_Eth>): FeePayer_Eth {
    const message = createBaseFeePayer_Eth();
    message.kind = object.kind ?? "";
    message.local = (object.local !== undefined && object.local !== null)
      ? FeePayer_Eth_Local.fromPartial(object.local)
      : undefined;
    message.remote = (object.remote !== undefined && object.remote !== null)
      ? FeePayer_Eth_Remote.fromPartial(object.remote)
      : undefined;
    message.remoteEncrypted = object.remoteEncrypted ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFeePayer_Eth_Local(): FeePayer_Eth_Local {
  return {
    addressId: 0,
    forwarderAddressId: 0,
    autoApprove: false,
    creatorAddressId: 0,
    forwarderKind: 0,
    domainSeparator: new Uint8Array(0),
  };
}

export const FeePayer_Eth_Local = {
  encode(message: FeePayer_Eth_Local, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.addressId !== 0) {
      writer.uint32(8).uint64(message.addressId);
    }
    if (message.forwarderAddressId !== 0) {
      writer.uint32(16).uint64(message.forwarderAddressId);
    }
    if (message.autoApprove !== false) {
      writer.uint32(24).bool(message.autoApprove);
    }
    if (message.creatorAddressId !== 0) {
      writer.uint32(32).uint64(message.creatorAddressId);
    }
    if (message.forwarderKind !== 0) {
      writer.uint32(40).int32(message.forwarderKind);
    }
    if (message.domainSeparator.length !== 0) {
      writer.uint32(50).bytes(message.domainSeparator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeePayer_Eth_Local {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeePayer_Eth_Local();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.addressId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.forwarderAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.autoApprove = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.creatorAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.forwarderKind = reader.int32() as any;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.domainSeparator = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeePayer_Eth_Local {
    return {
      addressId: isSet(object.addressId) ? globalThis.Number(object.addressId) : 0,
      forwarderAddressId: isSet(object.forwarderAddressId) ? globalThis.Number(object.forwarderAddressId) : 0,
      autoApprove: isSet(object.autoApprove) ? globalThis.Boolean(object.autoApprove) : false,
      creatorAddressId: isSet(object.creatorAddressId) ? globalThis.Number(object.creatorAddressId) : 0,
      forwarderKind: isSet(object.forwarderKind) ? feePayer_ForwarderKindFromJSON(object.forwarderKind) : 0,
      domainSeparator: isSet(object.domainSeparator) ? bytesFromBase64(object.domainSeparator) : new Uint8Array(0),
    };
  },

  toJSON(message: FeePayer_Eth_Local): unknown {
    const obj: any = {};
    if (message.addressId !== 0) {
      obj.addressId = Math.round(message.addressId);
    }
    if (message.forwarderAddressId !== 0) {
      obj.forwarderAddressId = Math.round(message.forwarderAddressId);
    }
    if (message.autoApprove !== false) {
      obj.autoApprove = message.autoApprove;
    }
    if (message.creatorAddressId !== 0) {
      obj.creatorAddressId = Math.round(message.creatorAddressId);
    }
    if (message.forwarderKind !== 0) {
      obj.forwarderKind = feePayer_ForwarderKindToJSON(message.forwarderKind);
    }
    if (message.domainSeparator.length !== 0) {
      obj.domainSeparator = base64FromBytes(message.domainSeparator);
    }
    return obj;
  },

  create(base?: DeepPartial<FeePayer_Eth_Local>): FeePayer_Eth_Local {
    return FeePayer_Eth_Local.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeePayer_Eth_Local>): FeePayer_Eth_Local {
    const message = createBaseFeePayer_Eth_Local();
    message.addressId = object.addressId ?? 0;
    message.forwarderAddressId = object.forwarderAddressId ?? 0;
    message.autoApprove = object.autoApprove ?? false;
    message.creatorAddressId = object.creatorAddressId ?? 0;
    message.forwarderKind = object.forwarderKind ?? 0;
    message.domainSeparator = object.domainSeparator ?? new Uint8Array(0);
    return message;
  },
};

function createBaseFeePayer_Eth_Remote(): FeePayer_Eth_Remote {
  return {
    url: "",
    username: "",
    password: "",
    privateKey: "",
    fromAddressId: 0,
    forwarderAddress: "",
    forwarderAddressId: 0,
    creatorAddress: "",
    creatorAddressId: 0,
    forwarderKind: 0,
    domainSeparator: new Uint8Array(0),
  };
}

export const FeePayer_Eth_Remote = {
  encode(message: FeePayer_Eth_Remote, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.password !== "") {
      writer.uint32(26).string(message.password);
    }
    if (message.privateKey !== "") {
      writer.uint32(34).string(message.privateKey);
    }
    if (message.fromAddressId !== 0) {
      writer.uint32(40).uint64(message.fromAddressId);
    }
    if (message.forwarderAddress !== "") {
      writer.uint32(50).string(message.forwarderAddress);
    }
    if (message.forwarderAddressId !== 0) {
      writer.uint32(56).uint64(message.forwarderAddressId);
    }
    if (message.creatorAddress !== "") {
      writer.uint32(66).string(message.creatorAddress);
    }
    if (message.creatorAddressId !== 0) {
      writer.uint32(72).uint64(message.creatorAddressId);
    }
    if (message.forwarderKind !== 0) {
      writer.uint32(80).int32(message.forwarderKind);
    }
    if (message.domainSeparator.length !== 0) {
      writer.uint32(90).bytes(message.domainSeparator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FeePayer_Eth_Remote {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeePayer_Eth_Remote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.password = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.privateKey = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.fromAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.forwarderAddress = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.forwarderAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.creatorAddress = reader.string();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.creatorAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.forwarderKind = reader.int32() as any;
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.domainSeparator = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeePayer_Eth_Remote {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      privateKey: isSet(object.privateKey) ? globalThis.String(object.privateKey) : "",
      fromAddressId: isSet(object.fromAddressId) ? globalThis.Number(object.fromAddressId) : 0,
      forwarderAddress: isSet(object.forwarderAddress) ? globalThis.String(object.forwarderAddress) : "",
      forwarderAddressId: isSet(object.forwarderAddressId) ? globalThis.Number(object.forwarderAddressId) : 0,
      creatorAddress: isSet(object.creatorAddress) ? globalThis.String(object.creatorAddress) : "",
      creatorAddressId: isSet(object.creatorAddressId) ? globalThis.Number(object.creatorAddressId) : 0,
      forwarderKind: isSet(object.forwarderKind) ? feePayer_ForwarderKindFromJSON(object.forwarderKind) : 0,
      domainSeparator: isSet(object.domainSeparator) ? bytesFromBase64(object.domainSeparator) : new Uint8Array(0),
    };
  },

  toJSON(message: FeePayer_Eth_Remote): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.privateKey !== "") {
      obj.privateKey = message.privateKey;
    }
    if (message.fromAddressId !== 0) {
      obj.fromAddressId = Math.round(message.fromAddressId);
    }
    if (message.forwarderAddress !== "") {
      obj.forwarderAddress = message.forwarderAddress;
    }
    if (message.forwarderAddressId !== 0) {
      obj.forwarderAddressId = Math.round(message.forwarderAddressId);
    }
    if (message.creatorAddress !== "") {
      obj.creatorAddress = message.creatorAddress;
    }
    if (message.creatorAddressId !== 0) {
      obj.creatorAddressId = Math.round(message.creatorAddressId);
    }
    if (message.forwarderKind !== 0) {
      obj.forwarderKind = feePayer_ForwarderKindToJSON(message.forwarderKind);
    }
    if (message.domainSeparator.length !== 0) {
      obj.domainSeparator = base64FromBytes(message.domainSeparator);
    }
    return obj;
  },

  create(base?: DeepPartial<FeePayer_Eth_Remote>): FeePayer_Eth_Remote {
    return FeePayer_Eth_Remote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FeePayer_Eth_Remote>): FeePayer_Eth_Remote {
    const message = createBaseFeePayer_Eth_Remote();
    message.url = object.url ?? "";
    message.username = object.username ?? "";
    message.password = object.password ?? "";
    message.privateKey = object.privateKey ?? "";
    message.fromAddressId = object.fromAddressId ?? 0;
    message.forwarderAddress = object.forwarderAddress ?? "";
    message.forwarderAddressId = object.forwarderAddressId ?? 0;
    message.creatorAddress = object.creatorAddress ?? "";
    message.creatorAddressId = object.creatorAddressId ?? 0;
    message.forwarderKind = object.forwarderKind ?? 0;
    message.domainSeparator = object.domainSeparator ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
