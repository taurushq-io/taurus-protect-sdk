// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: fee-service.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Currency } from "./currency";
import { Empty } from "./google/protobuf/empty";
import { Timestamp } from "./google/protobuf/timestamp";
import { KeyValue } from "./kv";

export const protobufPackage = "tgvalidatord";

export interface GetFeesReply {
  result: KeyValue[];
}

export interface GetFeesV2Reply {
  result: Fee[];
}

export interface Fee {
  currencyId: string;
  value: string;
  denom: string;
  currencyInfo: Currency | undefined;
  updateDate: Date | undefined;
}

function createBaseGetFeesReply(): GetFeesReply {
  return { result: [] };
}

export const GetFeesReply = {
  encode(message: GetFeesReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      KeyValue.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeesReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeesReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(KeyValue.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeesReply {
    return {
      result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => KeyValue.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetFeesReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => KeyValue.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeesReply>): GetFeesReply {
    return GetFeesReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeesReply>): GetFeesReply {
    const message = createBaseGetFeesReply();
    message.result = object.result?.map((e) => KeyValue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetFeesV2Reply(): GetFeesV2Reply {
  return { result: [] };
}

export const GetFeesV2Reply = {
  encode(message: GetFeesV2Reply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      Fee.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeesV2Reply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeesV2Reply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(Fee.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeesV2Reply {
    return { result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => Fee.fromJSON(e)) : [] };
  },

  toJSON(message: GetFeesV2Reply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => Fee.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeesV2Reply>): GetFeesV2Reply {
    return GetFeesV2Reply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeesV2Reply>): GetFeesV2Reply {
    const message = createBaseGetFeesV2Reply();
    message.result = object.result?.map((e) => Fee.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFee(): Fee {
  return { currencyId: "", value: "", denom: "", currencyInfo: undefined, updateDate: undefined };
}

export const Fee = {
  encode(message: Fee, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.currencyId !== "") {
      writer.uint32(10).string(message.currencyId);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    if (message.denom !== "") {
      writer.uint32(26).string(message.denom);
    }
    if (message.currencyInfo !== undefined) {
      Currency.encode(message.currencyInfo, writer.uint32(34).fork()).ldelim();
    }
    if (message.updateDate !== undefined) {
      Timestamp.encode(toTimestamp(message.updateDate), writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Fee {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFee();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.currencyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.denom = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.currencyInfo = Currency.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.updateDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fee {
    return {
      currencyId: isSet(object.currencyId) ? globalThis.String(object.currencyId) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      currencyInfo: isSet(object.currencyInfo) ? Currency.fromJSON(object.currencyInfo) : undefined,
      updateDate: isSet(object.updateDate) ? fromJsonTimestamp(object.updateDate) : undefined,
    };
  },

  toJSON(message: Fee): unknown {
    const obj: any = {};
    if (message.currencyId !== "") {
      obj.currencyId = message.currencyId;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.currencyInfo !== undefined) {
      obj.currencyInfo = Currency.toJSON(message.currencyInfo);
    }
    if (message.updateDate !== undefined) {
      obj.updateDate = message.updateDate.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Fee>): Fee {
    return Fee.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fee>): Fee {
    const message = createBaseFee();
    message.currencyId = object.currencyId ?? "";
    message.value = object.value ?? "";
    message.denom = object.denom ?? "";
    message.currencyInfo = (object.currencyInfo !== undefined && object.currencyInfo !== null)
      ? Currency.fromPartial(object.currencyInfo)
      : undefined;
    message.updateDate = object.updateDate ?? undefined;
    return message;
  },
};

export interface FeeService {
  /**
   * Deprecated - Replaced by GetFeesV2
   *
   * @deprecated
   */
  GetFees(request: Empty): Promise<GetFeesReply>;
  GetFeesV2(request: Empty): Promise<GetFeesV2Reply>;
}

export const FeeServiceServiceName = "tgvalidatord.FeeService";
export class FeeServiceClientImpl implements FeeService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || FeeServiceServiceName;
    this.rpc = rpc;
    this.GetFees = this.GetFees.bind(this);
    this.GetFeesV2 = this.GetFeesV2.bind(this);
  }
  GetFees(request: Empty): Promise<GetFeesReply> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetFees", data);
    return promise.then((data) => GetFeesReply.decode(_m0.Reader.create(data)));
  }

  GetFeesV2(request: Empty): Promise<GetFeesV2Reply> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetFeesV2", data);
    return promise.then((data) => GetFeesV2Reply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
