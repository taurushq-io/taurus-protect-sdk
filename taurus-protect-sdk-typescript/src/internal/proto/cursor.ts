// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: cursor.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

export const protobufPackage = "tgvalidatord";

export interface RequestCursor {
  /** Base64-encoded string representing the current window of data */
  currentPage: string;
  /**
   * The page to request, w.r.t the current page.
   * Can be one of `FIRST`, `PREVIOUS`, `NEXT`, `LAST`
   */
  pageRequest: string;
  /** The size of the page requested. The handling service should impose a hard limit on this */
  pageSize: number;
}

export interface ResponseCursor {
  /** Base64-encoded string representing the current window of data */
  currentPage: string;
  /**
   * Hints for UI to display whether a previous and/or next page of data
   * are available.
   */
  hasPrevious: boolean;
  hasNext: boolean;
}

function createBaseRequestCursor(): RequestCursor {
  return { currentPage: "", pageRequest: "", pageSize: 0 };
}

export const RequestCursor = {
  encode(message: RequestCursor, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.currentPage !== "") {
      writer.uint32(10).string(message.currentPage);
    }
    if (message.pageRequest !== "") {
      writer.uint32(18).string(message.pageRequest);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).uint64(message.pageSize);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RequestCursor {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestCursor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.currentPage = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pageRequest = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pageSize = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestCursor {
    return {
      currentPage: isSet(object.currentPage) ? globalThis.String(object.currentPage) : "",
      pageRequest: isSet(object.pageRequest) ? globalThis.String(object.pageRequest) : "",
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
    };
  },

  toJSON(message: RequestCursor): unknown {
    const obj: any = {};
    if (message.currentPage !== "") {
      obj.currentPage = message.currentPage;
    }
    if (message.pageRequest !== "") {
      obj.pageRequest = message.pageRequest;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    return obj;
  },

  create(base?: DeepPartial<RequestCursor>): RequestCursor {
    return RequestCursor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestCursor>): RequestCursor {
    const message = createBaseRequestCursor();
    message.currentPage = object.currentPage ?? "";
    message.pageRequest = object.pageRequest ?? "";
    message.pageSize = object.pageSize ?? 0;
    return message;
  },
};

function createBaseResponseCursor(): ResponseCursor {
  return { currentPage: "", hasPrevious: false, hasNext: false };
}

export const ResponseCursor = {
  encode(message: ResponseCursor, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.currentPage !== "") {
      writer.uint32(10).string(message.currentPage);
    }
    if (message.hasPrevious !== false) {
      writer.uint32(16).bool(message.hasPrevious);
    }
    if (message.hasNext !== false) {
      writer.uint32(24).bool(message.hasNext);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResponseCursor {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseCursor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.currentPage = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.hasPrevious = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.hasNext = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResponseCursor {
    return {
      currentPage: isSet(object.currentPage) ? globalThis.String(object.currentPage) : "",
      hasPrevious: isSet(object.hasPrevious) ? globalThis.Boolean(object.hasPrevious) : false,
      hasNext: isSet(object.hasNext) ? globalThis.Boolean(object.hasNext) : false,
    };
  },

  toJSON(message: ResponseCursor): unknown {
    const obj: any = {};
    if (message.currentPage !== "") {
      obj.currentPage = message.currentPage;
    }
    if (message.hasPrevious !== false) {
      obj.hasPrevious = message.hasPrevious;
    }
    if (message.hasNext !== false) {
      obj.hasNext = message.hasNext;
    }
    return obj;
  },

  create(base?: DeepPartial<ResponseCursor>): ResponseCursor {
    return ResponseCursor.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResponseCursor>): ResponseCursor {
    const message = createBaseResponseCursor();
    message.currentPage = object.currentPage ?? "";
    message.hasPrevious = object.hasPrevious ?? false;
    message.hasNext = object.hasNext ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
