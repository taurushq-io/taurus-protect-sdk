// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: feepayer-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { FeePayerEnvelope } from "./feepayer";

export const protobufPackage = "tgvalidatord";

export interface GetFeePayersRequest {
  limit: number;
  offset: number;
  ids: number[];
  blockchain: string;
  network: string;
}

export interface GetFeePayersReply {
  result: FeePayerEnvelope[];
  totalItems: number;
}

export interface GetFeePayerRequest {
  id: number;
}

export interface GetFeePayerReply {
  feepayer: FeePayerEnvelope | undefined;
}

export interface GetChecksumRequest {
  data: Uint8Array;
}

export interface GetChecksumReply {
  checksum: string;
}

export interface InitRemoteFeePayerRequest {
  tenantId: number;
  url: string;
  email: string;
  externalGroupId: string;
  password: string;
  userKey: string;
  encryptionKey: string;
  forwarderId: number;
  creatorId: number;
  feePayerAddressId: number;
  domainSeparator: Uint8Array;
}

export interface InitRemoteFeePayerReply {
  remoteEncrypted: Uint8Array;
  rulesUpdated: boolean;
  checksum: string;
}

function createBaseGetFeePayersRequest(): GetFeePayersRequest {
  return { limit: 0, offset: 0, ids: [], blockchain: "", network: "" };
}

export const GetFeePayersRequest = {
  encode(message: GetFeePayersRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.limit !== 0) {
      writer.uint32(8).uint64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint64(message.offset);
    }
    writer.uint32(26).fork();
    for (const v of message.ids) {
      writer.uint64(v);
    }
    writer.ldelim();
    if (message.blockchain !== "") {
      writer.uint32(34).string(message.blockchain);
    }
    if (message.network !== "") {
      writer.uint32(42).string(message.network);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeePayersRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeePayersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.limit = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.offset = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag === 24) {
            message.ids.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.network = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeePayersRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.Number(e)) : [],
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      network: isSet(object.network) ? globalThis.String(object.network) : "",
    };
  },

  toJSON(message: GetFeePayersRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e));
    }
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeePayersRequest>): GetFeePayersRequest {
    return GetFeePayersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeePayersRequest>): GetFeePayersRequest {
    const message = createBaseGetFeePayersRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.ids = object.ids?.map((e) => e) || [];
    message.blockchain = object.blockchain ?? "";
    message.network = object.network ?? "";
    return message;
  },
};

function createBaseGetFeePayersReply(): GetFeePayersReply {
  return { result: [], totalItems: 0 };
}

export const GetFeePayersReply = {
  encode(message: GetFeePayersReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      FeePayerEnvelope.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.totalItems !== 0) {
      writer.uint32(16).uint64(message.totalItems);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeePayersReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeePayersReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(FeePayerEnvelope.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.totalItems = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeePayersReply {
    return {
      result: globalThis.Array.isArray(object?.result)
        ? object.result.map((e: any) => FeePayerEnvelope.fromJSON(e))
        : [],
      totalItems: isSet(object.totalItems) ? globalThis.Number(object.totalItems) : 0,
    };
  },

  toJSON(message: GetFeePayersReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => FeePayerEnvelope.toJSON(e));
    }
    if (message.totalItems !== 0) {
      obj.totalItems = Math.round(message.totalItems);
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeePayersReply>): GetFeePayersReply {
    return GetFeePayersReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeePayersReply>): GetFeePayersReply {
    const message = createBaseGetFeePayersReply();
    message.result = object.result?.map((e) => FeePayerEnvelope.fromPartial(e)) || [];
    message.totalItems = object.totalItems ?? 0;
    return message;
  },
};

function createBaseGetFeePayerRequest(): GetFeePayerRequest {
  return { id: 0 };
}

export const GetFeePayerRequest = {
  encode(message: GetFeePayerRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeePayerRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeePayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeePayerRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: GetFeePayerRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeePayerRequest>): GetFeePayerRequest {
    return GetFeePayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeePayerRequest>): GetFeePayerRequest {
    const message = createBaseGetFeePayerRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGetFeePayerReply(): GetFeePayerReply {
  return { feepayer: undefined };
}

export const GetFeePayerReply = {
  encode(message: GetFeePayerReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.feepayer !== undefined) {
      FeePayerEnvelope.encode(message.feepayer, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetFeePayerReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFeePayerReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.feepayer = FeePayerEnvelope.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFeePayerReply {
    return { feepayer: isSet(object.feepayer) ? FeePayerEnvelope.fromJSON(object.feepayer) : undefined };
  },

  toJSON(message: GetFeePayerReply): unknown {
    const obj: any = {};
    if (message.feepayer !== undefined) {
      obj.feepayer = FeePayerEnvelope.toJSON(message.feepayer);
    }
    return obj;
  },

  create(base?: DeepPartial<GetFeePayerReply>): GetFeePayerReply {
    return GetFeePayerReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetFeePayerReply>): GetFeePayerReply {
    const message = createBaseGetFeePayerReply();
    message.feepayer = (object.feepayer !== undefined && object.feepayer !== null)
      ? FeePayerEnvelope.fromPartial(object.feepayer)
      : undefined;
    return message;
  },
};

function createBaseGetChecksumRequest(): GetChecksumRequest {
  return { data: new Uint8Array(0) };
}

export const GetChecksumRequest = {
  encode(message: GetChecksumRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChecksumRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChecksumRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChecksumRequest {
    return { data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0) };
  },

  toJSON(message: GetChecksumRequest): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChecksumRequest>): GetChecksumRequest {
    return GetChecksumRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChecksumRequest>): GetChecksumRequest {
    const message = createBaseGetChecksumRequest();
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetChecksumReply(): GetChecksumReply {
  return { checksum: "" };
}

export const GetChecksumReply = {
  encode(message: GetChecksumReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.checksum !== "") {
      writer.uint32(10).string(message.checksum);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChecksumReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChecksumReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.checksum = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChecksumReply {
    return { checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "" };
  },

  toJSON(message: GetChecksumReply): unknown {
    const obj: any = {};
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    return obj;
  },

  create(base?: DeepPartial<GetChecksumReply>): GetChecksumReply {
    return GetChecksumReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChecksumReply>): GetChecksumReply {
    const message = createBaseGetChecksumReply();
    message.checksum = object.checksum ?? "";
    return message;
  },
};

function createBaseInitRemoteFeePayerRequest(): InitRemoteFeePayerRequest {
  return {
    tenantId: 0,
    url: "",
    email: "",
    externalGroupId: "",
    password: "",
    userKey: "",
    encryptionKey: "",
    forwarderId: 0,
    creatorId: 0,
    feePayerAddressId: 0,
    domainSeparator: new Uint8Array(0),
  };
}

export const InitRemoteFeePayerRequest = {
  encode(message: InitRemoteFeePayerRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.tenantId !== 0) {
      writer.uint32(8).uint64(message.tenantId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.externalGroupId !== "") {
      writer.uint32(34).string(message.externalGroupId);
    }
    if (message.password !== "") {
      writer.uint32(42).string(message.password);
    }
    if (message.userKey !== "") {
      writer.uint32(50).string(message.userKey);
    }
    if (message.encryptionKey !== "") {
      writer.uint32(58).string(message.encryptionKey);
    }
    if (message.forwarderId !== 0) {
      writer.uint32(64).uint64(message.forwarderId);
    }
    if (message.creatorId !== 0) {
      writer.uint32(72).uint64(message.creatorId);
    }
    if (message.feePayerAddressId !== 0) {
      writer.uint32(80).uint64(message.feePayerAddressId);
    }
    if (message.domainSeparator.length !== 0) {
      writer.uint32(90).bytes(message.domainSeparator);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InitRemoteFeePayerRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitRemoteFeePayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.tenantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.externalGroupId = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.password = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.userKey = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.encryptionKey = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.forwarderId = longToNumber(reader.uint64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.creatorId = longToNumber(reader.uint64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.feePayerAddressId = longToNumber(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.domainSeparator = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitRemoteFeePayerRequest {
    return {
      tenantId: isSet(object.tenantId) ? globalThis.Number(object.tenantId) : 0,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      externalGroupId: isSet(object.externalGroupId) ? globalThis.String(object.externalGroupId) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      userKey: isSet(object.userKey) ? globalThis.String(object.userKey) : "",
      encryptionKey: isSet(object.encryptionKey) ? globalThis.String(object.encryptionKey) : "",
      forwarderId: isSet(object.forwarderId) ? globalThis.Number(object.forwarderId) : 0,
      creatorId: isSet(object.creatorId) ? globalThis.Number(object.creatorId) : 0,
      feePayerAddressId: isSet(object.feePayerAddressId) ? globalThis.Number(object.feePayerAddressId) : 0,
      domainSeparator: isSet(object.domainSeparator) ? bytesFromBase64(object.domainSeparator) : new Uint8Array(0),
    };
  },

  toJSON(message: InitRemoteFeePayerRequest): unknown {
    const obj: any = {};
    if (message.tenantId !== 0) {
      obj.tenantId = Math.round(message.tenantId);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.externalGroupId !== "") {
      obj.externalGroupId = message.externalGroupId;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.userKey !== "") {
      obj.userKey = message.userKey;
    }
    if (message.encryptionKey !== "") {
      obj.encryptionKey = message.encryptionKey;
    }
    if (message.forwarderId !== 0) {
      obj.forwarderId = Math.round(message.forwarderId);
    }
    if (message.creatorId !== 0) {
      obj.creatorId = Math.round(message.creatorId);
    }
    if (message.feePayerAddressId !== 0) {
      obj.feePayerAddressId = Math.round(message.feePayerAddressId);
    }
    if (message.domainSeparator.length !== 0) {
      obj.domainSeparator = base64FromBytes(message.domainSeparator);
    }
    return obj;
  },

  create(base?: DeepPartial<InitRemoteFeePayerRequest>): InitRemoteFeePayerRequest {
    return InitRemoteFeePayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InitRemoteFeePayerRequest>): InitRemoteFeePayerRequest {
    const message = createBaseInitRemoteFeePayerRequest();
    message.tenantId = object.tenantId ?? 0;
    message.url = object.url ?? "";
    message.email = object.email ?? "";
    message.externalGroupId = object.externalGroupId ?? "";
    message.password = object.password ?? "";
    message.userKey = object.userKey ?? "";
    message.encryptionKey = object.encryptionKey ?? "";
    message.forwarderId = object.forwarderId ?? 0;
    message.creatorId = object.creatorId ?? 0;
    message.feePayerAddressId = object.feePayerAddressId ?? 0;
    message.domainSeparator = object.domainSeparator ?? new Uint8Array(0);
    return message;
  },
};

function createBaseInitRemoteFeePayerReply(): InitRemoteFeePayerReply {
  return { remoteEncrypted: new Uint8Array(0), rulesUpdated: false, checksum: "" };
}

export const InitRemoteFeePayerReply = {
  encode(message: InitRemoteFeePayerReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.remoteEncrypted.length !== 0) {
      writer.uint32(10).bytes(message.remoteEncrypted);
    }
    if (message.rulesUpdated !== false) {
      writer.uint32(16).bool(message.rulesUpdated);
    }
    if (message.checksum !== "") {
      writer.uint32(26).string(message.checksum);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): InitRemoteFeePayerReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInitRemoteFeePayerReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.remoteEncrypted = reader.bytes();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.rulesUpdated = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.checksum = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InitRemoteFeePayerReply {
    return {
      remoteEncrypted: isSet(object.remoteEncrypted) ? bytesFromBase64(object.remoteEncrypted) : new Uint8Array(0),
      rulesUpdated: isSet(object.rulesUpdated) ? globalThis.Boolean(object.rulesUpdated) : false,
      checksum: isSet(object.checksum) ? globalThis.String(object.checksum) : "",
    };
  },

  toJSON(message: InitRemoteFeePayerReply): unknown {
    const obj: any = {};
    if (message.remoteEncrypted.length !== 0) {
      obj.remoteEncrypted = base64FromBytes(message.remoteEncrypted);
    }
    if (message.rulesUpdated !== false) {
      obj.rulesUpdated = message.rulesUpdated;
    }
    if (message.checksum !== "") {
      obj.checksum = message.checksum;
    }
    return obj;
  },

  create(base?: DeepPartial<InitRemoteFeePayerReply>): InitRemoteFeePayerReply {
    return InitRemoteFeePayerReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InitRemoteFeePayerReply>): InitRemoteFeePayerReply {
    const message = createBaseInitRemoteFeePayerReply();
    message.remoteEncrypted = object.remoteEncrypted ?? new Uint8Array(0);
    message.rulesUpdated = object.rulesUpdated ?? false;
    message.checksum = object.checksum ?? "";
    return message;
  },
};

export interface FeePayerService {
  GetFeePayers(request: GetFeePayersRequest): Promise<GetFeePayersReply>;
  GetFeePayer(request: GetFeePayerRequest): Promise<GetFeePayerReply>;
  /** It is important that this is defined AFTER the more generic /api/rest/v1/feepayers/{id} routes */
  Checksum(request: GetChecksumRequest): Promise<GetChecksumReply>;
  InitRemoteFeePayer(request: InitRemoteFeePayerRequest): Promise<InitRemoteFeePayerReply>;
}

export const FeePayerServiceServiceName = "tgvalidatord.FeePayerService";
export class FeePayerServiceClientImpl implements FeePayerService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || FeePayerServiceServiceName;
    this.rpc = rpc;
    this.GetFeePayers = this.GetFeePayers.bind(this);
    this.GetFeePayer = this.GetFeePayer.bind(this);
    this.Checksum = this.Checksum.bind(this);
    this.InitRemoteFeePayer = this.InitRemoteFeePayer.bind(this);
  }
  GetFeePayers(request: GetFeePayersRequest): Promise<GetFeePayersReply> {
    const data = GetFeePayersRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetFeePayers", data);
    return promise.then((data) => GetFeePayersReply.decode(_m0.Reader.create(data)));
  }

  GetFeePayer(request: GetFeePayerRequest): Promise<GetFeePayerReply> {
    const data = GetFeePayerRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetFeePayer", data);
    return promise.then((data) => GetFeePayerReply.decode(_m0.Reader.create(data)));
  }

  Checksum(request: GetChecksumRequest): Promise<GetChecksumReply> {
    const data = GetChecksumRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Checksum", data);
    return promise.then((data) => GetChecksumReply.decode(_m0.Reader.create(data)));
  }

  InitRemoteFeePayer(request: InitRemoteFeePayerRequest): Promise<InitRemoteFeePayerReply> {
    const data = InitRemoteFeePayerRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "InitRemoteFeePayer", data);
    return promise.then((data) => InitRemoteFeePayerReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
