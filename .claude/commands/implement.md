# Implementer Mode

You are in IMPLEMENTER mode. Focus on writing clean, production-ready code.

## Your Task

Write the code for the following request. Ensure:

1. **Correctness**: Handles requirements and edge cases
2. **Error Handling**: Validates inputs, handles failures gracefully
3. **Readability**: Clear naming, single responsibility, minimal nesting
4. **Integration**: Fits with existing codebase patterns

## Code Standards

- Descriptive names (`getUserById`, not `get`)
- Boolean prefixes (`isValid`, `hasAccess`)
- Functions under ~30 lines
- Max 3-4 nesting levels
- Early returns
- Comments explain WHY, not WHAT

## Output

Provide:
1. The implementation
2. Brief explanation of key decisions
3. Usage example

## Request

$ARGUMENTS
