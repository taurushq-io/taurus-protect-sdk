// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: sso-service.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { SecurityDomain } from "./sso";

export const protobufPackage = "tgvalidatord";

export interface GetSecurityDomainsRequest {
}

export interface GetSecurityDomainsReply {
  result: SecurityDomain[];
}

function createBaseGetSecurityDomainsRequest(): GetSecurityDomainsRequest {
  return {};
}

export const GetSecurityDomainsRequest = {
  encode(_: GetSecurityDomainsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetSecurityDomainsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSecurityDomainsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSecurityDomainsRequest {
    return {};
  },

  toJSON(_: GetSecurityDomainsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetSecurityDomainsRequest>): GetSecurityDomainsRequest {
    return GetSecurityDomainsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetSecurityDomainsRequest>): GetSecurityDomainsRequest {
    const message = createBaseGetSecurityDomainsRequest();
    return message;
  },
};

function createBaseGetSecurityDomainsReply(): GetSecurityDomainsReply {
  return { result: [] };
}

export const GetSecurityDomainsReply = {
  encode(message: GetSecurityDomainsReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      SecurityDomain.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetSecurityDomainsReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSecurityDomainsReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(SecurityDomain.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSecurityDomainsReply {
    return {
      result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => SecurityDomain.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetSecurityDomainsReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => SecurityDomain.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetSecurityDomainsReply>): GetSecurityDomainsReply {
    return GetSecurityDomainsReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSecurityDomainsReply>): GetSecurityDomainsReply {
    const message = createBaseGetSecurityDomainsReply();
    message.result = object.result?.map((e) => SecurityDomain.fromPartial(e)) || [];
    return message;
  },
};

export interface SecurityDomainService {
  GetSecurityDomains(request: GetSecurityDomainsRequest): Promise<GetSecurityDomainsReply>;
}

export const SecurityDomainServiceServiceName = "tgvalidatord.SecurityDomainService";
export class SecurityDomainServiceClientImpl implements SecurityDomainService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || SecurityDomainServiceServiceName;
    this.rpc = rpc;
    this.GetSecurityDomains = this.GetSecurityDomains.bind(this);
  }
  GetSecurityDomains(request: GetSecurityDomainsRequest): Promise<GetSecurityDomainsReply> {
    const data = GetSecurityDomainsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetSecurityDomains", data);
    return promise.then((data) => GetSecurityDomainsReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;
