// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: auth_proxy/api_request_to_validate.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "auth_proxy";

export interface ApiRequestToValidate {
  prefix: string;
  apiKey: string;
  nonce: string;
  timestamp: Date | undefined;
  method: string;
  host: string;
  path: string;
  query: string;
  contentType: string;
  body: Uint8Array;
  signature: Uint8Array;
}

function createBaseApiRequestToValidate(): ApiRequestToValidate {
  return {
    prefix: "",
    apiKey: "",
    nonce: "",
    timestamp: undefined,
    method: "",
    host: "",
    path: "",
    query: "",
    contentType: "",
    body: new Uint8Array(0),
    signature: new Uint8Array(0),
  };
}

export const ApiRequestToValidate = {
  encode(message: ApiRequestToValidate, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    if (message.apiKey !== "") {
      writer.uint32(18).string(message.apiKey);
    }
    if (message.nonce !== "") {
      writer.uint32(26).string(message.nonce);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).ldelim();
    }
    if (message.method !== "") {
      writer.uint32(42).string(message.method);
    }
    if (message.host !== "") {
      writer.uint32(50).string(message.host);
    }
    if (message.path !== "") {
      writer.uint32(58).string(message.path);
    }
    if (message.query !== "") {
      writer.uint32(66).string(message.query);
    }
    if (message.contentType !== "") {
      writer.uint32(74).string(message.contentType);
    }
    if (message.body.length !== 0) {
      writer.uint32(82).bytes(message.body);
    }
    if (message.signature.length !== 0) {
      writer.uint32(90).bytes(message.signature);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ApiRequestToValidate {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiRequestToValidate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.nonce = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.method = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.host = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.path = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.query = reader.string();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.contentType = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.body = reader.bytes();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.signature = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApiRequestToValidate {
    return {
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      apiKey: isSet(object.apiKey) ? globalThis.String(object.apiKey) : "",
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      contentType: isSet(object.contentType) ? globalThis.String(object.contentType) : "",
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: ApiRequestToValidate): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.apiKey !== "") {
      obj.apiKey = message.apiKey;
    }
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create(base?: DeepPartial<ApiRequestToValidate>): ApiRequestToValidate {
    return ApiRequestToValidate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApiRequestToValidate>): ApiRequestToValidate {
    const message = createBaseApiRequestToValidate();
    message.prefix = object.prefix ?? "";
    message.apiKey = object.apiKey ?? "";
    message.nonce = object.nonce ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.method = object.method ?? "";
    message.host = object.host ?? "";
    message.path = object.path ?? "";
    message.query = object.query ?? "";
    message.contentType = object.contentType ?? "";
    message.body = object.body ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
