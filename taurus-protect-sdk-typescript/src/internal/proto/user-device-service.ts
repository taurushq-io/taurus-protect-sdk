// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: user-device-service.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Empty } from "./google/protobuf/empty";
import { UserDevicePairingInfo } from "./user-device";

export const protobufPackage = "tgvalidatord";

export interface CreateUserDevicePairingReply {
  pairingID: string;
}

export interface StartUserDevicePairingRequest {
  pairingID: string;
  nonce: string;
  publicKey: string;
}

export interface UserDevicePairingRequest {
  pairingID: string;
  nonce: string;
}

function createBaseCreateUserDevicePairingReply(): CreateUserDevicePairingReply {
  return { pairingID: "" };
}

export const CreateUserDevicePairingReply = {
  encode(message: CreateUserDevicePairingReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pairingID !== "") {
      writer.uint32(10).string(message.pairingID);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateUserDevicePairingReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateUserDevicePairingReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pairingID = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateUserDevicePairingReply {
    return { pairingID: isSet(object.pairingID) ? globalThis.String(object.pairingID) : "" };
  },

  toJSON(message: CreateUserDevicePairingReply): unknown {
    const obj: any = {};
    if (message.pairingID !== "") {
      obj.pairingID = message.pairingID;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateUserDevicePairingReply>): CreateUserDevicePairingReply {
    return CreateUserDevicePairingReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateUserDevicePairingReply>): CreateUserDevicePairingReply {
    const message = createBaseCreateUserDevicePairingReply();
    message.pairingID = object.pairingID ?? "";
    return message;
  },
};

function createBaseStartUserDevicePairingRequest(): StartUserDevicePairingRequest {
  return { pairingID: "", nonce: "", publicKey: "" };
}

export const StartUserDevicePairingRequest = {
  encode(message: StartUserDevicePairingRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pairingID !== "") {
      writer.uint32(10).string(message.pairingID);
    }
    if (message.nonce !== "") {
      writer.uint32(18).string(message.nonce);
    }
    if (message.publicKey !== "") {
      writer.uint32(26).string(message.publicKey);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartUserDevicePairingRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartUserDevicePairingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pairingID = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nonce = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.publicKey = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartUserDevicePairingRequest {
    return {
      pairingID: isSet(object.pairingID) ? globalThis.String(object.pairingID) : "",
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "",
      publicKey: isSet(object.publicKey) ? globalThis.String(object.publicKey) : "",
    };
  },

  toJSON(message: StartUserDevicePairingRequest): unknown {
    const obj: any = {};
    if (message.pairingID !== "") {
      obj.pairingID = message.pairingID;
    }
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    return obj;
  },

  create(base?: DeepPartial<StartUserDevicePairingRequest>): StartUserDevicePairingRequest {
    return StartUserDevicePairingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartUserDevicePairingRequest>): StartUserDevicePairingRequest {
    const message = createBaseStartUserDevicePairingRequest();
    message.pairingID = object.pairingID ?? "";
    message.nonce = object.nonce ?? "";
    message.publicKey = object.publicKey ?? "";
    return message;
  },
};

function createBaseUserDevicePairingRequest(): UserDevicePairingRequest {
  return { pairingID: "", nonce: "" };
}

export const UserDevicePairingRequest = {
  encode(message: UserDevicePairingRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.pairingID !== "") {
      writer.uint32(10).string(message.pairingID);
    }
    if (message.nonce !== "") {
      writer.uint32(18).string(message.nonce);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UserDevicePairingRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserDevicePairingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pairingID = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nonce = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserDevicePairingRequest {
    return {
      pairingID: isSet(object.pairingID) ? globalThis.String(object.pairingID) : "",
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "",
    };
  },

  toJSON(message: UserDevicePairingRequest): unknown {
    const obj: any = {};
    if (message.pairingID !== "") {
      obj.pairingID = message.pairingID;
    }
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    return obj;
  },

  create(base?: DeepPartial<UserDevicePairingRequest>): UserDevicePairingRequest {
    return UserDevicePairingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserDevicePairingRequest>): UserDevicePairingRequest {
    const message = createBaseUserDevicePairingRequest();
    message.pairingID = object.pairingID ?? "";
    message.nonce = object.nonce ?? "";
    return message;
  },
};

export interface UserDeviceService {
  StartUserDevicePairing(request: StartUserDevicePairingRequest): Promise<Empty>;
  ApproveUserDevicePairing(request: UserDevicePairingRequest): Promise<Empty>;
  GetUserDevicePairingStatus(request: UserDevicePairingRequest): Promise<UserDevicePairingInfo>;
  CreateUserDevicePairing(request: Empty): Promise<CreateUserDevicePairingReply>;
}

export const UserDeviceServiceServiceName = "tgvalidatord.UserDeviceService";
export class UserDeviceServiceClientImpl implements UserDeviceService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || UserDeviceServiceServiceName;
    this.rpc = rpc;
    this.StartUserDevicePairing = this.StartUserDevicePairing.bind(this);
    this.ApproveUserDevicePairing = this.ApproveUserDevicePairing.bind(this);
    this.GetUserDevicePairingStatus = this.GetUserDevicePairingStatus.bind(this);
    this.CreateUserDevicePairing = this.CreateUserDevicePairing.bind(this);
  }
  StartUserDevicePairing(request: StartUserDevicePairingRequest): Promise<Empty> {
    const data = StartUserDevicePairingRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "StartUserDevicePairing", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  ApproveUserDevicePairing(request: UserDevicePairingRequest): Promise<Empty> {
    const data = UserDevicePairingRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApproveUserDevicePairing", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  GetUserDevicePairingStatus(request: UserDevicePairingRequest): Promise<UserDevicePairingInfo> {
    const data = UserDevicePairingRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetUserDevicePairingStatus", data);
    return promise.then((data) => UserDevicePairingInfo.decode(_m0.Reader.create(data)));
  }

  CreateUserDevicePairing(request: Empty): Promise<CreateUserDevicePairingReply> {
    const data = Empty.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateUserDevicePairing", data);
    return promise.then((data) => CreateUserDevicePairingReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
