// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: action-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { ActionEnvelope } from "./action";

export const protobufPackage = "tgvalidatord";

export interface GetActionsRequest {
  limit: number;
  offset: number;
  ids: number[];
}

export interface GetActionsReply {
  result: ActionEnvelope[];
  totalItems: number;
}

export interface GetActionRequest {
  id: number;
}

export interface GetActionReply {
  action: ActionEnvelope | undefined;
}

function createBaseGetActionsRequest(): GetActionsRequest {
  return { limit: 0, offset: 0, ids: [] };
}

export const GetActionsRequest = {
  encode(message: GetActionsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.limit !== 0) {
      writer.uint32(8).uint64(message.limit);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint64(message.offset);
    }
    writer.uint32(26).fork();
    for (const v of message.ids) {
      writer.uint64(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetActionsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.limit = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.offset = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag === 24) {
            message.ids.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActionsRequest {
    return {
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: GetActionsRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetActionsRequest>): GetActionsRequest {
    return GetActionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetActionsRequest>): GetActionsRequest {
    const message = createBaseGetActionsRequest();
    message.limit = object.limit ?? 0;
    message.offset = object.offset ?? 0;
    message.ids = object.ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetActionsReply(): GetActionsReply {
  return { result: [], totalItems: 0 };
}

export const GetActionsReply = {
  encode(message: GetActionsReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      ActionEnvelope.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.totalItems !== 0) {
      writer.uint32(16).uint64(message.totalItems);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetActionsReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActionsReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(ActionEnvelope.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.totalItems = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActionsReply {
    return {
      result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => ActionEnvelope.fromJSON(e)) : [],
      totalItems: isSet(object.totalItems) ? globalThis.Number(object.totalItems) : 0,
    };
  },

  toJSON(message: GetActionsReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => ActionEnvelope.toJSON(e));
    }
    if (message.totalItems !== 0) {
      obj.totalItems = Math.round(message.totalItems);
    }
    return obj;
  },

  create(base?: DeepPartial<GetActionsReply>): GetActionsReply {
    return GetActionsReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetActionsReply>): GetActionsReply {
    const message = createBaseGetActionsReply();
    message.result = object.result?.map((e) => ActionEnvelope.fromPartial(e)) || [];
    message.totalItems = object.totalItems ?? 0;
    return message;
  },
};

function createBaseGetActionRequest(): GetActionRequest {
  return { id: 0 };
}

export const GetActionRequest = {
  encode(message: GetActionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetActionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActionRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: GetActionRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<GetActionRequest>): GetActionRequest {
    return GetActionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetActionRequest>): GetActionRequest {
    const message = createBaseGetActionRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGetActionReply(): GetActionReply {
  return { action: undefined };
}

export const GetActionReply = {
  encode(message: GetActionReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.action !== undefined) {
      ActionEnvelope.encode(message.action, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetActionReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActionReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.action = ActionEnvelope.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetActionReply {
    return { action: isSet(object.action) ? ActionEnvelope.fromJSON(object.action) : undefined };
  },

  toJSON(message: GetActionReply): unknown {
    const obj: any = {};
    if (message.action !== undefined) {
      obj.action = ActionEnvelope.toJSON(message.action);
    }
    return obj;
  },

  create(base?: DeepPartial<GetActionReply>): GetActionReply {
    return GetActionReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetActionReply>): GetActionReply {
    const message = createBaseGetActionReply();
    message.action = (object.action !== undefined && object.action !== null)
      ? ActionEnvelope.fromPartial(object.action)
      : undefined;
    return message;
  },
};

export interface ActionService {
  GetActions(request: GetActionsRequest): Promise<GetActionsReply>;
  GetAction(request: GetActionRequest): Promise<GetActionReply>;
}

export const ActionServiceServiceName = "tgvalidatord.ActionService";
export class ActionServiceClientImpl implements ActionService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ActionServiceServiceName;
    this.rpc = rpc;
    this.GetActions = this.GetActions.bind(this);
    this.GetAction = this.GetAction.bind(this);
  }
  GetActions(request: GetActionsRequest): Promise<GetActionsReply> {
    const data = GetActionsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetActions", data);
    return promise.then((data) => GetActionsReply.decode(_m0.Reader.create(data)));
  }

  GetAction(request: GetActionRequest): Promise<GetActionReply> {
    const data = GetActionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetAction", data);
    return promise.then((data) => GetActionReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
