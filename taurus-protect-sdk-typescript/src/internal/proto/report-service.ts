// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: report-service.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { HttpBody } from "./google/api/httpbody";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "tgvalidatord";

export interface SwiftMT535BalanceReportRequest {
  blockchain: string;
  blockchainaddress: string;
  forDate: Date | undefined;
  network: string;
}

export interface SwiftMT536TransactionsReportRequest {
  blockchain: string;
  blockchainaddress: string;
  forDate: Date | undefined;
  network: string;
}

function createBaseSwiftMT535BalanceReportRequest(): SwiftMT535BalanceReportRequest {
  return { blockchain: "", blockchainaddress: "", forDate: undefined, network: "" };
}

export const SwiftMT535BalanceReportRequest = {
  encode(message: SwiftMT535BalanceReportRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.blockchain !== "") {
      writer.uint32(10).string(message.blockchain);
    }
    if (message.blockchainaddress !== "") {
      writer.uint32(18).string(message.blockchainaddress);
    }
    if (message.forDate !== undefined) {
      Timestamp.encode(toTimestamp(message.forDate), writer.uint32(26).fork()).ldelim();
    }
    if (message.network !== "") {
      writer.uint32(34).string(message.network);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SwiftMT535BalanceReportRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSwiftMT535BalanceReportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.blockchainaddress = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.forDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.network = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SwiftMT535BalanceReportRequest {
    return {
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      blockchainaddress: isSet(object.blockchainaddress) ? globalThis.String(object.blockchainaddress) : "",
      forDate: isSet(object.forDate) ? fromJsonTimestamp(object.forDate) : undefined,
      network: isSet(object.network) ? globalThis.String(object.network) : "",
    };
  },

  toJSON(message: SwiftMT535BalanceReportRequest): unknown {
    const obj: any = {};
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.blockchainaddress !== "") {
      obj.blockchainaddress = message.blockchainaddress;
    }
    if (message.forDate !== undefined) {
      obj.forDate = message.forDate.toISOString();
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    return obj;
  },

  create(base?: DeepPartial<SwiftMT535BalanceReportRequest>): SwiftMT535BalanceReportRequest {
    return SwiftMT535BalanceReportRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SwiftMT535BalanceReportRequest>): SwiftMT535BalanceReportRequest {
    const message = createBaseSwiftMT535BalanceReportRequest();
    message.blockchain = object.blockchain ?? "";
    message.blockchainaddress = object.blockchainaddress ?? "";
    message.forDate = object.forDate ?? undefined;
    message.network = object.network ?? "";
    return message;
  },
};

function createBaseSwiftMT536TransactionsReportRequest(): SwiftMT536TransactionsReportRequest {
  return { blockchain: "", blockchainaddress: "", forDate: undefined, network: "" };
}

export const SwiftMT536TransactionsReportRequest = {
  encode(message: SwiftMT536TransactionsReportRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.blockchain !== "") {
      writer.uint32(10).string(message.blockchain);
    }
    if (message.blockchainaddress !== "") {
      writer.uint32(18).string(message.blockchainaddress);
    }
    if (message.forDate !== undefined) {
      Timestamp.encode(toTimestamp(message.forDate), writer.uint32(26).fork()).ldelim();
    }
    if (message.network !== "") {
      writer.uint32(34).string(message.network);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SwiftMT536TransactionsReportRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSwiftMT536TransactionsReportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.blockchain = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.blockchainaddress = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.forDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.network = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SwiftMT536TransactionsReportRequest {
    return {
      blockchain: isSet(object.blockchain) ? globalThis.String(object.blockchain) : "",
      blockchainaddress: isSet(object.blockchainaddress) ? globalThis.String(object.blockchainaddress) : "",
      forDate: isSet(object.forDate) ? fromJsonTimestamp(object.forDate) : undefined,
      network: isSet(object.network) ? globalThis.String(object.network) : "",
    };
  },

  toJSON(message: SwiftMT536TransactionsReportRequest): unknown {
    const obj: any = {};
    if (message.blockchain !== "") {
      obj.blockchain = message.blockchain;
    }
    if (message.blockchainaddress !== "") {
      obj.blockchainaddress = message.blockchainaddress;
    }
    if (message.forDate !== undefined) {
      obj.forDate = message.forDate.toISOString();
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    return obj;
  },

  create(base?: DeepPartial<SwiftMT536TransactionsReportRequest>): SwiftMT536TransactionsReportRequest {
    return SwiftMT536TransactionsReportRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SwiftMT536TransactionsReportRequest>): SwiftMT536TransactionsReportRequest {
    const message = createBaseSwiftMT536TransactionsReportRequest();
    message.blockchain = object.blockchain ?? "";
    message.blockchainaddress = object.blockchainaddress ?? "";
    message.forDate = object.forDate ?? undefined;
    message.network = object.network ?? "";
    return message;
  },
};

export interface ReportService {
  SwiftMT535BalanceReport(request: SwiftMT535BalanceReportRequest): Promise<HttpBody>;
  SwiftMT536TransactionsReport(request: SwiftMT536TransactionsReportRequest): Promise<HttpBody>;
}

export const ReportServiceServiceName = "tgvalidatord.ReportService";
export class ReportServiceClientImpl implements ReportService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ReportServiceServiceName;
    this.rpc = rpc;
    this.SwiftMT535BalanceReport = this.SwiftMT535BalanceReport.bind(this);
    this.SwiftMT536TransactionsReport = this.SwiftMT536TransactionsReport.bind(this);
  }
  SwiftMT535BalanceReport(request: SwiftMT535BalanceReportRequest): Promise<HttpBody> {
    const data = SwiftMT535BalanceReportRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SwiftMT535BalanceReport", data);
    return promise.then((data) => HttpBody.decode(_m0.Reader.create(data)));
  }

  SwiftMT536TransactionsReport(request: SwiftMT536TransactionsReportRequest): Promise<HttpBody> {
    const data = SwiftMT536TransactionsReportRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SwiftMT536TransactionsReport", data);
    return promise.then((data) => HttpBody.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
