// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v6.33.4
// source: change-service.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Change, Change_Status, change_StatusFromJSON, change_StatusToJSON } from "./change";
import { RequestCursor, ResponseCursor } from "./cursor";
import { Empty } from "./google/protobuf/empty";

export const protobufPackage = "tgvalidatord";

export interface CreateChangeRequest {
  action: string;
  entityId: number;
  entity: string;
  changes: { [key: string]: string };
  changeComment: string;
  entityUUID: string;
}

export interface CreateChangeRequest_ChangesEntry {
  key: string;
  value: string;
}

export interface CreateChangeReply {
  result: CreateChangeResult | undefined;
}

export interface CreateChangeResult {
  id: number;
}

export interface GetChangesRequest {
  entity: string;
  /** @deprecated */
  entityId: number;
  status: Change_Status;
  creatorId: number;
  sortOrder: string;
  cursor: RequestCursor | undefined;
  entityIDs: number[];
  entityUUIDs: string[];
}

export interface GetChangeRequest {
  id: number;
}

export interface GetChangeReply {
  result: Change | undefined;
}

export interface GetChangesReply {
  result: Change[];
  cursor: ResponseCursor | undefined;
}

export interface ApproveChangeRequest {
  id: number;
}

export interface ApproveChangesRequest {
  ids: number[];
}

export interface RejectChangeRequest {
  id: number;
}

export interface RejectChangesRequest {
  ids: number[];
}

export interface GetChangesForApprovalRequest {
  entities: string[];
  sortOrder: string;
  cursor: RequestCursor | undefined;
  entityIDs: number[];
  entityUUIDs: string[];
}

function createBaseCreateChangeRequest(): CreateChangeRequest {
  return { action: "", entityId: 0, entity: "", changes: {}, changeComment: "", entityUUID: "" };
}

export const CreateChangeRequest = {
  encode(message: CreateChangeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.action !== "") {
      writer.uint32(10).string(message.action);
    }
    if (message.entityId !== 0) {
      writer.uint32(16).uint64(message.entityId);
    }
    if (message.entity !== "") {
      writer.uint32(26).string(message.entity);
    }
    Object.entries(message.changes).forEach(([key, value]) => {
      CreateChangeRequest_ChangesEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).ldelim();
    });
    if (message.changeComment !== "") {
      writer.uint32(42).string(message.changeComment);
    }
    if (message.entityUUID !== "") {
      writer.uint32(50).string(message.entityUUID);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateChangeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.action = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.entityId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.entity = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          const entry4 = CreateChangeRequest_ChangesEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.changes[entry4.key] = entry4.value;
          }
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.changeComment = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.entityUUID = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeRequest {
    return {
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      entityId: isSet(object.entityId) ? globalThis.Number(object.entityId) : 0,
      entity: isSet(object.entity) ? globalThis.String(object.entity) : "",
      changes: isObject(object.changes)
        ? Object.entries(object.changes).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      changeComment: isSet(object.changeComment) ? globalThis.String(object.changeComment) : "",
      entityUUID: isSet(object.entityUUID) ? globalThis.String(object.entityUUID) : "",
    };
  },

  toJSON(message: CreateChangeRequest): unknown {
    const obj: any = {};
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.entityId !== 0) {
      obj.entityId = Math.round(message.entityId);
    }
    if (message.entity !== "") {
      obj.entity = message.entity;
    }
    if (message.changes) {
      const entries = Object.entries(message.changes);
      if (entries.length > 0) {
        obj.changes = {};
        entries.forEach(([k, v]) => {
          obj.changes[k] = v;
        });
      }
    }
    if (message.changeComment !== "") {
      obj.changeComment = message.changeComment;
    }
    if (message.entityUUID !== "") {
      obj.entityUUID = message.entityUUID;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeRequest>): CreateChangeRequest {
    return CreateChangeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeRequest>): CreateChangeRequest {
    const message = createBaseCreateChangeRequest();
    message.action = object.action ?? "";
    message.entityId = object.entityId ?? 0;
    message.entity = object.entity ?? "";
    message.changes = Object.entries(object.changes ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.changeComment = object.changeComment ?? "";
    message.entityUUID = object.entityUUID ?? "";
    return message;
  },
};

function createBaseCreateChangeRequest_ChangesEntry(): CreateChangeRequest_ChangesEntry {
  return { key: "", value: "" };
}

export const CreateChangeRequest_ChangesEntry = {
  encode(message: CreateChangeRequest_ChangesEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateChangeRequest_ChangesEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeRequest_ChangesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeRequest_ChangesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateChangeRequest_ChangesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeRequest_ChangesEntry>): CreateChangeRequest_ChangesEntry {
    return CreateChangeRequest_ChangesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeRequest_ChangesEntry>): CreateChangeRequest_ChangesEntry {
    const message = createBaseCreateChangeRequest_ChangesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateChangeReply(): CreateChangeReply {
  return { result: undefined };
}

export const CreateChangeReply = {
  encode(message: CreateChangeReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.result !== undefined) {
      CreateChangeResult.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateChangeReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result = CreateChangeResult.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeReply {
    return { result: isSet(object.result) ? CreateChangeResult.fromJSON(object.result) : undefined };
  },

  toJSON(message: CreateChangeReply): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = CreateChangeResult.toJSON(message.result);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeReply>): CreateChangeReply {
    return CreateChangeReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeReply>): CreateChangeReply {
    const message = createBaseCreateChangeReply();
    message.result = (object.result !== undefined && object.result !== null)
      ? CreateChangeResult.fromPartial(object.result)
      : undefined;
    return message;
  },
};

function createBaseCreateChangeResult(): CreateChangeResult {
  return { id: 0 };
}

export const CreateChangeResult = {
  encode(message: CreateChangeResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CreateChangeResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChangeResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChangeResult {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: CreateChangeResult): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChangeResult>): CreateChangeResult {
    return CreateChangeResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChangeResult>): CreateChangeResult {
    const message = createBaseCreateChangeResult();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGetChangesRequest(): GetChangesRequest {
  return {
    entity: "",
    entityId: 0,
    status: 0,
    creatorId: 0,
    sortOrder: "",
    cursor: undefined,
    entityIDs: [],
    entityUUIDs: [],
  };
}

export const GetChangesRequest = {
  encode(message: GetChangesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.entity !== "") {
      writer.uint32(10).string(message.entity);
    }
    if (message.entityId !== 0) {
      writer.uint32(16).uint64(message.entityId);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.creatorId !== 0) {
      writer.uint32(32).uint64(message.creatorId);
    }
    if (message.sortOrder !== "") {
      writer.uint32(42).string(message.sortOrder);
    }
    if (message.cursor !== undefined) {
      RequestCursor.encode(message.cursor, writer.uint32(50).fork()).ldelim();
    }
    writer.uint32(58).fork();
    for (const v of message.entityIDs) {
      writer.uint64(v);
    }
    writer.ldelim();
    for (const v of message.entityUUIDs) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChangesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.entity = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.entityId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.creatorId = longToNumber(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.sortOrder = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.cursor = RequestCursor.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag === 56) {
            message.entityIDs.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.entityIDs.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.entityUUIDs.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangesRequest {
    return {
      entity: isSet(object.entity) ? globalThis.String(object.entity) : "",
      entityId: isSet(object.entityId) ? globalThis.Number(object.entityId) : 0,
      status: isSet(object.status) ? change_StatusFromJSON(object.status) : 0,
      creatorId: isSet(object.creatorId) ? globalThis.Number(object.creatorId) : 0,
      sortOrder: isSet(object.sortOrder) ? globalThis.String(object.sortOrder) : "",
      cursor: isSet(object.cursor) ? RequestCursor.fromJSON(object.cursor) : undefined,
      entityIDs: globalThis.Array.isArray(object?.entityIDs)
        ? object.entityIDs.map((e: any) => globalThis.Number(e))
        : [],
      entityUUIDs: globalThis.Array.isArray(object?.entityUUIDs)
        ? object.entityUUIDs.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetChangesRequest): unknown {
    const obj: any = {};
    if (message.entity !== "") {
      obj.entity = message.entity;
    }
    if (message.entityId !== 0) {
      obj.entityId = Math.round(message.entityId);
    }
    if (message.status !== 0) {
      obj.status = change_StatusToJSON(message.status);
    }
    if (message.creatorId !== 0) {
      obj.creatorId = Math.round(message.creatorId);
    }
    if (message.sortOrder !== "") {
      obj.sortOrder = message.sortOrder;
    }
    if (message.cursor !== undefined) {
      obj.cursor = RequestCursor.toJSON(message.cursor);
    }
    if (message.entityIDs?.length) {
      obj.entityIDs = message.entityIDs.map((e) => Math.round(e));
    }
    if (message.entityUUIDs?.length) {
      obj.entityUUIDs = message.entityUUIDs;
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangesRequest>): GetChangesRequest {
    return GetChangesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangesRequest>): GetChangesRequest {
    const message = createBaseGetChangesRequest();
    message.entity = object.entity ?? "";
    message.entityId = object.entityId ?? 0;
    message.status = object.status ?? 0;
    message.creatorId = object.creatorId ?? 0;
    message.sortOrder = object.sortOrder ?? "";
    message.cursor = (object.cursor !== undefined && object.cursor !== null)
      ? RequestCursor.fromPartial(object.cursor)
      : undefined;
    message.entityIDs = object.entityIDs?.map((e) => e) || [];
    message.entityUUIDs = object.entityUUIDs?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetChangeRequest(): GetChangeRequest {
  return { id: 0 };
}

export const GetChangeRequest = {
  encode(message: GetChangeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChangeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangeRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: GetChangeRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangeRequest>): GetChangeRequest {
    return GetChangeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangeRequest>): GetChangeRequest {
    const message = createBaseGetChangeRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGetChangeReply(): GetChangeReply {
  return { result: undefined };
}

export const GetChangeReply = {
  encode(message: GetChangeReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.result !== undefined) {
      Change.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChangeReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangeReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result = Change.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangeReply {
    return { result: isSet(object.result) ? Change.fromJSON(object.result) : undefined };
  },

  toJSON(message: GetChangeReply): unknown {
    const obj: any = {};
    if (message.result !== undefined) {
      obj.result = Change.toJSON(message.result);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangeReply>): GetChangeReply {
    return GetChangeReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangeReply>): GetChangeReply {
    const message = createBaseGetChangeReply();
    message.result = (object.result !== undefined && object.result !== null)
      ? Change.fromPartial(object.result)
      : undefined;
    return message;
  },
};

function createBaseGetChangesReply(): GetChangesReply {
  return { result: [], cursor: undefined };
}

export const GetChangesReply = {
  encode(message: GetChangesReply, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.result) {
      Change.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.cursor !== undefined) {
      ResponseCursor.encode(message.cursor, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChangesReply {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangesReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.result.push(Change.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.cursor = ResponseCursor.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangesReply {
    return {
      result: globalThis.Array.isArray(object?.result) ? object.result.map((e: any) => Change.fromJSON(e)) : [],
      cursor: isSet(object.cursor) ? ResponseCursor.fromJSON(object.cursor) : undefined,
    };
  },

  toJSON(message: GetChangesReply): unknown {
    const obj: any = {};
    if (message.result?.length) {
      obj.result = message.result.map((e) => Change.toJSON(e));
    }
    if (message.cursor !== undefined) {
      obj.cursor = ResponseCursor.toJSON(message.cursor);
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangesReply>): GetChangesReply {
    return GetChangesReply.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangesReply>): GetChangesReply {
    const message = createBaseGetChangesReply();
    message.result = object.result?.map((e) => Change.fromPartial(e)) || [];
    message.cursor = (object.cursor !== undefined && object.cursor !== null)
      ? ResponseCursor.fromPartial(object.cursor)
      : undefined;
    return message;
  },
};

function createBaseApproveChangeRequest(): ApproveChangeRequest {
  return { id: 0 };
}

export const ApproveChangeRequest = {
  encode(message: ApproveChangeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ApproveChangeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveChangeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveChangeRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: ApproveChangeRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<ApproveChangeRequest>): ApproveChangeRequest {
    return ApproveChangeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApproveChangeRequest>): ApproveChangeRequest {
    const message = createBaseApproveChangeRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseApproveChangesRequest(): ApproveChangesRequest {
  return { ids: [] };
}

export const ApproveChangesRequest = {
  encode(message: ApproveChangesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.ids) {
      writer.uint64(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ApproveChangesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApproveChangesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.ids.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApproveChangesRequest {
    return { ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: ApproveChangesRequest): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ApproveChangesRequest>): ApproveChangesRequest {
    return ApproveChangesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApproveChangesRequest>): ApproveChangesRequest {
    const message = createBaseApproveChangesRequest();
    message.ids = object.ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseRejectChangeRequest(): RejectChangeRequest {
  return { id: 0 };
}

export const RejectChangeRequest = {
  encode(message: RejectChangeRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RejectChangeRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectChangeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectChangeRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: RejectChangeRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<RejectChangeRequest>): RejectChangeRequest {
    return RejectChangeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RejectChangeRequest>): RejectChangeRequest {
    const message = createBaseRejectChangeRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseRejectChangesRequest(): RejectChangesRequest {
  return { ids: [] };
}

export const RejectChangesRequest = {
  encode(message: RejectChangesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.ids) {
      writer.uint64(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RejectChangesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectChangesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.ids.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectChangesRequest {
    return { ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: RejectChangesRequest): unknown {
    const obj: any = {};
    if (message.ids?.length) {
      obj.ids = message.ids.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RejectChangesRequest>): RejectChangesRequest {
    return RejectChangesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RejectChangesRequest>): RejectChangesRequest {
    const message = createBaseRejectChangesRequest();
    message.ids = object.ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetChangesForApprovalRequest(): GetChangesForApprovalRequest {
  return { entities: [], sortOrder: "", cursor: undefined, entityIDs: [], entityUUIDs: [] };
}

export const GetChangesForApprovalRequest = {
  encode(message: GetChangesForApprovalRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.entities) {
      writer.uint32(10).string(v!);
    }
    if (message.sortOrder !== "") {
      writer.uint32(18).string(message.sortOrder);
    }
    if (message.cursor !== undefined) {
      RequestCursor.encode(message.cursor, writer.uint32(26).fork()).ldelim();
    }
    writer.uint32(34).fork();
    for (const v of message.entityIDs) {
      writer.uint64(v);
    }
    writer.ldelim();
    for (const v of message.entityUUIDs) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetChangesForApprovalRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChangesForApprovalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.entities.push(reader.string());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.sortOrder = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.cursor = RequestCursor.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag === 32) {
            message.entityIDs.push(longToNumber(reader.uint64() as Long));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.entityIDs.push(longToNumber(reader.uint64() as Long));
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.entityUUIDs.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChangesForApprovalRequest {
    return {
      entities: globalThis.Array.isArray(object?.entities) ? object.entities.map((e: any) => globalThis.String(e)) : [],
      sortOrder: isSet(object.sortOrder) ? globalThis.String(object.sortOrder) : "",
      cursor: isSet(object.cursor) ? RequestCursor.fromJSON(object.cursor) : undefined,
      entityIDs: globalThis.Array.isArray(object?.entityIDs)
        ? object.entityIDs.map((e: any) => globalThis.Number(e))
        : [],
      entityUUIDs: globalThis.Array.isArray(object?.entityUUIDs)
        ? object.entityUUIDs.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetChangesForApprovalRequest): unknown {
    const obj: any = {};
    if (message.entities?.length) {
      obj.entities = message.entities;
    }
    if (message.sortOrder !== "") {
      obj.sortOrder = message.sortOrder;
    }
    if (message.cursor !== undefined) {
      obj.cursor = RequestCursor.toJSON(message.cursor);
    }
    if (message.entityIDs?.length) {
      obj.entityIDs = message.entityIDs.map((e) => Math.round(e));
    }
    if (message.entityUUIDs?.length) {
      obj.entityUUIDs = message.entityUUIDs;
    }
    return obj;
  },

  create(base?: DeepPartial<GetChangesForApprovalRequest>): GetChangesForApprovalRequest {
    return GetChangesForApprovalRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetChangesForApprovalRequest>): GetChangesForApprovalRequest {
    const message = createBaseGetChangesForApprovalRequest();
    message.entities = object.entities?.map((e) => e) || [];
    message.sortOrder = object.sortOrder ?? "";
    message.cursor = (object.cursor !== undefined && object.cursor !== null)
      ? RequestCursor.fromPartial(object.cursor)
      : undefined;
    message.entityIDs = object.entityIDs?.map((e) => e) || [];
    message.entityUUIDs = object.entityUUIDs?.map((e) => e) || [];
    return message;
  },
};

export interface ChangeService {
  GetChanges(request: GetChangesRequest): Promise<GetChangesReply>;
  GetChange(request: GetChangeRequest): Promise<GetChangeReply>;
  CreateChange(request: CreateChangeRequest): Promise<CreateChangeReply>;
  ApproveChange(request: ApproveChangeRequest): Promise<Empty>;
  ApproveChanges(request: ApproveChangesRequest): Promise<Empty>;
  RejectChange(request: RejectChangeRequest): Promise<Empty>;
  /** It is important that this is defined AFTER the more generic /api/rest/v1/changes/{id}/reject routes */
  RejectChanges(request: RejectChangesRequest): Promise<Empty>;
  /** It is important that this is defined AFTER the more generic /api/rest/v1/changes/{id} routes */
  GetChangesForApproval(request: GetChangesForApprovalRequest): Promise<GetChangesReply>;
}

export const ChangeServiceServiceName = "tgvalidatord.ChangeService";
export class ChangeServiceClientImpl implements ChangeService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ChangeServiceServiceName;
    this.rpc = rpc;
    this.GetChanges = this.GetChanges.bind(this);
    this.GetChange = this.GetChange.bind(this);
    this.CreateChange = this.CreateChange.bind(this);
    this.ApproveChange = this.ApproveChange.bind(this);
    this.ApproveChanges = this.ApproveChanges.bind(this);
    this.RejectChange = this.RejectChange.bind(this);
    this.RejectChanges = this.RejectChanges.bind(this);
    this.GetChangesForApproval = this.GetChangesForApproval.bind(this);
  }
  GetChanges(request: GetChangesRequest): Promise<GetChangesReply> {
    const data = GetChangesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetChanges", data);
    return promise.then((data) => GetChangesReply.decode(_m0.Reader.create(data)));
  }

  GetChange(request: GetChangeRequest): Promise<GetChangeReply> {
    const data = GetChangeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetChange", data);
    return promise.then((data) => GetChangeReply.decode(_m0.Reader.create(data)));
  }

  CreateChange(request: CreateChangeRequest): Promise<CreateChangeReply> {
    const data = CreateChangeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateChange", data);
    return promise.then((data) => CreateChangeReply.decode(_m0.Reader.create(data)));
  }

  ApproveChange(request: ApproveChangeRequest): Promise<Empty> {
    const data = ApproveChangeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApproveChange", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  ApproveChanges(request: ApproveChangesRequest): Promise<Empty> {
    const data = ApproveChangesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ApproveChanges", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  RejectChange(request: RejectChangeRequest): Promise<Empty> {
    const data = RejectChangeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RejectChange", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  RejectChanges(request: RejectChangesRequest): Promise<Empty> {
    const data = RejectChangesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RejectChanges", data);
    return promise.then((data) => Empty.decode(_m0.Reader.create(data)));
  }

  GetChangesForApproval(request: GetChangesForApprovalRequest): Promise<GetChangesReply> {
    const data = GetChangesForApprovalRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetChangesForApproval", data);
    return promise.then((data) => GetChangesReply.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
